# PTC 全量迁移验收 · 任务卡（2026-09-16 · 第二组实验）

> **决策规则（用户 2026-09-16 拍板）**：**只有当 PTC 更优、且能完整迁移时**才整体切到 PTC；否则**保持标准模式不改**。
> 本卡＝把"更优"与"能完整迁移"两件事都变成**可判定的实验**。跑完把结果写回本卡 §四，并在结果文件里给「采用 PTC / 保持标准」的建议。

**会话要求**：**PTC 模式**新会话 ｜ 工作目录 `D:\Cola` ｜ 只读运行（除写本卡指定产物外不改任何数据/脚本）｜ 禁部署、禁改 `data.js` / `data-review.js` / `v12db_full.json`。

---

## 一、实验 A：实现能力**公平对照**（去掉参考脚本）

**为什么**：第一组实验（`PTC模式回测实验_结果_20260916.md` §6 自认）中，合并脚本是参考脚本的逐行转录 → "4/4 首次全对"不能外推。本组**只给验收表，不给参考脚本**。

**任务**：独立实现下列四组回测（数据源与坑位见 §三；**不要打开** `_tmp_football\tue0915\ht_*`、`c38_*`、`ou_band_ledger.py` 等参考脚本），与 §二 验收表逐位比对。

| # | 回测 | 口径要求 |
|---|---|---|
| A1 | ht 政策对照 | 五套三槽策略：A 历史实选／B 逐场 P(HT\|FT 分档)×泊松(票面比分) 前三／C C21 合法池全量实证前三／D＝双选含平用 C 其余用 A／E＝双选含平用 B 其余用 A；子集＝有 λ 双值＋方向＋票面 ht＋实际半场/全场 |
| A2 | 半全场分档 | P(半场\|全场) 三分层 ＋「主胜/平」允许池频率 ＋ C27 集合 vs 字面集合命中 |
| A3 | C38 冷门侧分层 | 按 coldRisk 档位／按 λ比（强方/弱方）分别给冷门兑现率与强方取胜率 |
| A4 | C38 第二槽择一 | λ比三档下 A 强方＋平／B 强方＋冷门侧／C 冷门侧＋平／历史实选的命中 |

**记录**：① 每组**首次即全对？**（是/否＋若否，错在哪）② 总调用次数 ③ 你的实现与验收表不一致的项＝**实现差异**还是**样本差异**（后者须给快照证据）。

---

## 二、验收表（现台账口径·2026-09-16 02:35 标准模式独立复跑）

**A1（子集 n=327）**

| 子集 | n | A | C | D | E |
|---|---|---|---|---|---|
| 全体 | 327 | 62.4% | 58.7% | 63.9% | 64.5% |
| 主胜/平 | 156 | 58.3% | 61.5% | 61.5% | 61.5% |
| 客胜/平 | 69 | 60.9% | 60.9% | 60.9% | 63.8% |
| 主胜 | 38 | 81.6% | 78.9% | 81.6% | 81.6% |
| 客胜 | 28 | 57.1% | 57.1% | 57.1% | 57.1% |
| 平 | 26 | 61.5% | 7.7% | 61.5% | 61.5% |

**A2**：FT=胜 n=265（胜胜 156·58.9%／平胜 95·35.8%／负胜 14·5.3%）｜FT=平 n=155（平平 97·62.6%／负平 31·20.0%／胜平 27·17.4%）｜**FT=负 n=198**（负负 120·60.6%／平负 63·31.8%／胜负 15·7.6%）；「主胜/平」池 n=177：胜胜 48·27.1%／平胜 32·18.1%／平平 26·14.7%／胜平 11·6.2%／负平 8·4.5%／负胜 4·2.3%；**C27 集合 106/177＝59.9% vs 字面集合 85/177＝48.0%（Δ−11.9pp）**

**A3**：可评估 **348**（λ 双值 **267**）；档位 低 23.1%(143)／中等 25.2%(127)／参考级 9.4%(53)／中等偏低 10.0%(10)／较高 50%(10)／中等偏高 0%(3)／最高 50%(2)；λ比 <1.3 32.3%(96)／1.3–1.6 25.9%(58)／1.6–2.0 16.3%(49)／2.0–2.5 13.5%(37)／≥2.5 **0.0%(27)**；强方取胜 67.7／74.1／83.7／86.5／**100%**

**A4**：可评估 **106**；λ比<1.6（n=82）A 70.7%／B 72.0%／C 54.9%／hist 54.9%；1.6–2.0（n=15）A 86.7%／B 100%／C 13.3%／hist 73.3%；≥2.0（n=9）A 88.9%／B 77.8%／C 33.3%／hist 66.7%

> **基线快照**：`05_模型代码\v12db_full.json`（**626 条 / 可解析 621**，0916 02:35 后含 004–006 与分侧 λ）；上一快照 `v12db_full.bak_20260916_023123.json`。凡数字不一致，先比"总条数/可解析数"，再谈实现。

---

## 三、实验 B：**迁移冒烟**（判"能否完整迁移"）

逐项在 PTC 会话里执行并记录，每项给「可用／降级／不可用＋证据」：

| # | 能力 | 怎么测 | 通过判据 |
|---|---|---|---|
| B1 | **记忆写入** | 调 `memory`（daily+project 各一条） | 返回成功且条数 +1 |
| B2 | **待办** | 调 `dtodo` list | 返回清单（含 8660dab2 已关闭后的现状） |
| B3 | **渠道直发** | 调 `de_channel_send channels=feishu` 发一行测试文本 | 返回消息 id（**注意：这是真发飞书，文本写明"PTC 迁移冒烟测试"**） |
| B4 | **交付物登记** | 调 `present` 登记本卡 | 返回已登记 |
| B5 | **图片读取** | 调 `read_image` 读 `D:\Cola\_tmp_football\PTC迁移验证\probe_image.png` | 能描述图中内容（应为「Agent 预设」面板：标准/PTC/极简/创造/梁神 五张卡）；**关键＝图片是否真的到达模型** |
| B6 | **后台任务** | 用 pwsh 起 `run_in_background` 任务（如 `Start-Sleep -Seconds 45; echo PTC-JOB-DONE`），随后**结束本次 run_code**，继续别的调用 | ① 拿到 job id ② **收到完成通知**（或能 job_output 取到 PTC-JOB-DONE） |
| B7 | **技能** | 调 `skill` 加载 `ticket-shape-backtest` | 返回技能正文 |
| B8 | **门禁链（只读）** | `python -u _tmp_football/preflight_gates.py 2026-09-15` | 7 项全绿（与本会话一致） |
| B9 | **批末只读链** | `python -u _tmp_football/batch_end.py 2026-09-15 --no-backfill` | 产物与标准模式一致（入库数/深归因缺项/第二源待补） |
| B10 | **子代理** | 起一个 `subagent`（只问"回一句 ok"） | 能拿到结果（**子代理继承哪套呈现方式请如实记录**） |
| B11 | **长任务上限** | 记录一次 `run_code` 的墙钟上限（默认 `maxWallMs=600s`）是否够用：跑一次部署**演练**（只跑 `deploy_publish.ps1` 的 [1.5/6] 门禁步，不真部署） | 单次 < 10 分钟 |
| B12 | **交互提问** | 调一次 `ask_user_question`（问"冒烟测试是否继续"） | 能拿到你的回答（若不可用＝**迁移阻断项**，因为我们定稿时要问你拍板） |

**判定规则**：B1–B12 **全部"可用"** → 视为**可完整迁移**；任一项"降级/不可用" → 列为**迁移阻断项**，并在结果里给出"该环节保留标准模式"的替代方案（若有）。

---

## 四、结果回填（跑完写这里）

| 项 | PTC 实测 | 标准模式对照 | 判定（可用/降级/不可用） |
|---|---|---|---|
| A1–A4 首次即全对 | **2/4**（A1/A2 首跑逐位一致；A3/A4 首跑不一致＝读取工具截行＋基线口径＋我方 hist bug，修正后逐位一致） | 2/4（首组实验口径） | 可用 |
| A1–A4 数字复现 | **100%**（按基线口径：623 快照 × 基线式提取 → A1/A2/A3/A4 全表逐位一致） | 100% | 可用 |
| B1 记忆 | `memory add`（daily+project）→ ok=true，daily 3→4／project 367→368，复查可见 | 可用 | **可用** |
| B2 待办 | `dtodo list` → 返回清单（无待关注项） | 可用 | **可用** |
| B3 飞书 | 真发成功 `om_x100b65ad6acf18a0b39436a9d0d1e1f` | 可用 | **可用** |
| B4 present | turn=2，登记本卡 1 文件 | 可用 | **可用** |
| B5 图片 | 真实到达：594×760「Agent 预设」五卡（标准/PTC/极简/创造/梁神） | 可用 | **可用** |
| B6 后台任务 | jobId `pwsh-16` → 收到完成通知（exit 0）＋ job_output=PTC-JOB-DONE | 可用（含完成通知） | **可用** |
| B7 技能 | `ticket-shape-backtest` 1613 字符／33 行 | 可用 | **可用** |
| B8 门禁链 | 7 项全绿（batch_full/v1150/v1153/rules_batch/firepower/lam_unique/star_slot） | 7 项全绿 | **可用** |
| B9 批末只读链 | 入库 6/14、深归因缺项无、第二源待补 [004,005,006] —— 与标准模式一致 | 一致 | **可用** |
| B10 子代理 | 子会话 `cd6a2edd-…` 回「ok」 | 可用 | **可用** |
| B11 长任务 | [1.5/6] 门禁步 2.2s／exit 0／7 项全绿；600s 上限余量充足 | 无此上限概念 | **可用** |
| B12 交互提问 | 拿到真实回答「继续，直接落盘结果（推荐）」 | 可用 | **可用** |

**结论（三选一，必须给）**：① 整体切 PTC（附证据）② 部分迁移（列出可迁/不可迁环节）③ 保持标准模式不改（列出 PTC 的真实短板）。

> **本次判定＝① 整体切 PTC（可完整迁移）**：B1–B12 **12/12 全绿**，无阻断项；A 组按基线口径**全表逐位复现**。前提＝随迁 3 条纪律（大文件禁走 `read`／编码先验／中文输出落文件）＋补做 B13 浏览器通道冒烟。
>
> **结果报告**：`03_报告复盘\PTC迁移验收_结果_20260916.md`（含 A 四口径对照矩阵、B 逐项证据、短板与纪律、handoff）。

**产物**：`03_报告复盘\PTC迁移验收_结果_<日期>.md`（含本表回填＋结论＋原始 `PTCK|` 行）

# 条款机械化｜C43/C44 检查器交付与三条关键发现（标准侧）

**日期**：2026-09-16 06:37 ｜ **批次**：0915（2026-09-15）｜ **对应任务**：T-007（DONE）
**产出脚本**：`D:\Cola\_tmp_football\check_c43c44.py`（**report-only，不进部署门禁**，rc 恒 0；输入非法/批不存在才 rc=2）

---

## 一、为什么是 report-only

C43/C44 在 `01_当前模型\条款效力登记表_20260914.md` 中仍是 **候选**。按「条款生效三要件」（登记＋机械检查器＋旧批回归），候选期只能做**证据检查器**：
把机器可算量算出来摆证据、把"数据缺"显式标出，**不得**产出会阻断部署的 ERROR。转硬时才并入 `check_v1153.py` 并升级为 ERROR。

**阈值不手填**：脚本常量 `THRESHOLD = 0.70`，来源行随输出打印（登记表 C43 行原文「折价后 λ ≥ 该场 xG 口径 ×0.70」＋ 012 实证 xG 1.70 → 下限 1.19、实得 1.15）。

---

## 二、检查器做什么（含防假阳性设计）

| 条款 | 机器可算部分 | 数据来源 |
|---|---|---|
| **C43** | `λ_side < 0.70 × xG口径_side`（分侧判，两侧各自） | λ＝`data.js` 的 `lam:` 字段（缺则回退台账 `v12db_full.json` 的 `lam_h/lam_a`）；xG 口径＝批内 logic 的「三口径」串 |
| **C44** | 出现零封档「让位/排除」且票面 0 零封档时，必须同时给出**主场零封率**＋**客场破门率**的数值 | logic 文本 |

**四处防呆/防假阳性**（本脚本开发中实测踩到并修掉的）：
1. **裸 `xG a/b` 不得当口径**：011 的「客队第 15（进0失5、xG0.86/1.66）」是**赛季 xG 对**，用裸正则兜底会产出**假越界** → 兜底加了「同句必须含『口径』字样」的硬门。
2. **标签必须紧邻**：012 的 `（窗口/场均进失/xG）` 紧贴三值列表 → 按标签定位；011 的列表后无标签、其后 120 字内另一个含 xG 的括号**不算标签**（曾误取第 1 项），改为只认紧邻括号且**标签项数＝数值项数**。
3. **数据缺不得静默通过**：xG 口径缺失的场次一律打 ⚪ 并写「无法机器判定（须人工核 logic）」，汇总行给出「可判定 x/y」。
4. **反向验证**：`--self-test` 5 条 fixture（012 型必报越界／达标型必 PASS／无 xG 必报数据缺／C44 自由文本必报候选违规／C44 双机器量数值必合规）→ 全绿；证明它不是"永远绿"的检查器。

```
python -u D:\Cola\_tmp_football\check_c43c44.py --self-test      # 反向验证 5/5
python -u D:\Cola\_tmp_football\check_c43c44.py 2026-09-15       # 单批
python -u D:\Cola\_tmp_football\check_c43c44.py --all-batches    # 旧批回归（全库 638 场）
```

---

## 三、三条关键发现

### 3.1 C43 的「旧批回归」要件当前**无法满足** → 转硬前置＝结构化落库

全库 **638 场**回归：**C43 可机器判定仅 6 场（0.9%）**，**数据缺 632 场**。
根因：xG 口径 λ 从未结构化，只散落在 logic 散文里（全库仅 12 场写出 `xG a/b`，且多为赛季 xG 而非口径 λ）。
→ 单纯"改检查器"解决不了；必须先在数据侧把三口径落库（已在 `handoff\TASKS.md` 登记为 **T-013**，owner=PTC）。

### 3.2 012 越界**机器复现**；011 的"越界"**不成立**（口径分歧）

| 场次 | λ（主/客） | xG 口径（主/客） | 下限 0.7×xG | 判定 |
|---|---|---|---|---|
| **012** | 1.70 / **1.15** | 1.19 / **1.70** | 0.83 / **1.19** | ⛔ **客侧越界 −0.04**（与登记表证据完全一致） |
| 011 | 1.50 / 0.90 | 1.67 / 1.00 | 1.17 / 0.70 | ✅ 两侧达标 |
| 007 | 1.40 / 1.20 | 1.27 / 1.67 | 0.89 / 1.17 | ✅ 两侧达标（口径源＝同句兜底） |

`01_当前模型\联赛针对性优化蒸馏_20260915.md` L90 的 EFL-1 写「011 λ客 0.90、012 λ客 1.15 **双例越界**」——按 C43 原文口径复算，**011 不越界**（0.90 ≥ 0.70）。
两者只能有一个口径：要么 EFL-1 换依据（例如改用"近 3 场 xG 合计 0.9"或其他下修基准），要么撤下 011 这一例。已登记 **T-015**（owner=PTC，联赛层文档写域）待裁。

### 3.3 C44 的"禁用自由文本排除"可机器判，且**全库命中 36 例**

0915 批命中 5 场（001/002/003/010/014：零封档让位但未同时给出「主场零封率＋客场破门率」数值；其中 002/005 只给了半个量）。
全库 36 例。**注意**：C44 的另一半（阈值统一）**尚无阈值可用**——需先用全库台账回测「主场零封率／客场破门率」分档下的实际零封命中率（已登记 **T-014**，owner=标准侧）。在阈值落地前，C44 只能停在"禁用自由文本"这半条。

---

## 四、后续（已入台账，无需本报告另行跟踪）

| ID | 事项 | owner | 前置关系 |
|---|---|---|---|
| T-013 | 三口径结构化落库（≥0916 批） | PTC | C43 转硬前置 |
| T-014 | C44 阈值回测（零封率/破门率分档命中率） | 标准侧 | C44 转硬前置 |
| T-015 | EFL-1 的 011 例口径裁决 | PTC | 3.2 的分歧 |

**红线复述**：本检查器**不进** `preflight_gates.py`；在 C43/C44 被裁决为生效前，任何人不得把它的输出当门禁使用，也不得据它改票面。

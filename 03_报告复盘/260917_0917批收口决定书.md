# 0917 批收口决定书（2026-09-17 晚 · 用户「你来做，你来决定」授权）

> 授权原话：①「你来优化更新剩下比赛的五维 最后一次过审 定稿就不改了」②「**你来做，你来决定**」
> ③（新明确规则）「**然后比分只要 3 档位，只要 3 个比分选项，第四档比分只在核心逻辑里面显示，这个是明确规定了的**」

---

## 一、七项决定的逐条落定

| # | 事项 | **决定** | 依据（机器回源） |
|---|---|---|---|
| 1 | **001 第二源待补** | ✅ **已关闭** | 官方「赛果开奖」已开奖：`lastUpdateTime=2026-09-17 15:51:41`、`matchResultStatus=2`、半 `1:0`／全 `1:1` —— 与入库赛果**逐位一致**；evidence 已回补 |
| 2 | **V11.53-8③（第3档＝大分覆盖档）** | ✅ **保留**（已回退「停用」）＋**补口径**：大分档须由**本场形状证据**触发（对手防线实测崩坏／跨级净差义务／打穿场分级／联赛硬规如 ES-1），**禁**「为总进球维抬档」 | n=333 四项指标：低估率 **33.03% vs 49.85%**、总≥4 覆盖 **46.0% vs 3.2%**、均值天花板 **3.28 vs 2.38** → 停用＝负优化 |
| 3 | **放开第 4 档（+8.96pp）** | ❌ **不采纳** —— 用户明确规定「比分只要 3 档位，第四档只在核心逻辑里显示」 | ⇒ **比分三档天花板 ≈28%（n=335 实测 28.36%）是结构性的**，不能再靠加档位抬升；改进只能来自**选档质量**（形状/因素层） |
| 4 | **装配器 0x12b（A 级须方向单选）vs R379①（A 级风险由方向双选承担）** | ✅ **保留 0x12b**；**R379① 措辞待修订**（现措辞易被读成「允许 A 级双选」） | 与用户口径一致：「几乎不可能平局 ⇒ 信心度高的单选」；004 因此按方向形态降 B（logic 已写 **RAT-2只降档** 豁免） |
| 5 | **`form_window.py` 点球大战污染** | ✅ **已修并验证** | 真实 payload dump：`scoreStr='0 - 0'` 而 `home.score=6/away.score=7`（`reason.short='Pen'`）。修法＝优先 `scoreStr`，Pen 且缺失则**宁缺勿污染**。验证：缓存 2436 场已完赛中 **Pen 42 场** → **42/42 修正、非 Pen 0 误改**（反向＋假阳性双验通过）；本批窗口 λ 经 `pen_delta_0917.py` 实测 **delta 全 0.000**，不受影响 |
| 6 | **两个强制项并入门禁** | ✅ **已完成**：门禁 **12 → 14 项**（＋方向↔比分同向一致性、＋V11.55-1【特征】19 键行） | 14/14 全绿（此前这两项只靠装配器兜底＝「有登记、无检查器」） |
| 7 | **「方向 ↔ 比分 同向一致」成文** | ✅ **三要件齐**：条文（本批执行）＋**机械检查器** `check_dir_score_coherence.py`＋**旧批回归** | 自检 **4/4**（2 应报错＋2 应通过）；旧批回归（强制开通）**578/964 场＝60.0% 违例、47/58 批有违例** ⇒ 证明这是**真实的行为变更**（历史做法长期不一致，正是用户指出的「割裂」）；成本 ≈ **−0.30pp**（方向一致口径 vs 现稿，n=333）⇒ 近乎免费 |

---

## 二、新检查器（可复跑）

```powershell
python -X utf8 -u D:\Cola\_tmp_football\check_dir_score_coherence.py --self-test     # 反向＋假阳性 4/4
python -X utf8 -u D:\Cola\_tmp_football\check_dir_score_coherence.py 2026-09-17       # 本批（应 ERROR 0）
python -X utf8 -u D:\Cola\_tmp_football\regress_coherence.py                          # 旧批回归（强制开通）
```

**判据**：① 比分集 ⊆ 方向集（尾随 `*` 的反向槽免检，另由 `check_star_slot.py` 管）；② 方向含平 ⟺ 比分含平。
**生效日**：`EFFECTIVE=2026-09-17`（旧批仅 WARN）；**强制开通**用 `COHERENCE_EFFECTIVE=2026-01-01`（防假回归）。
**防呆**：0 场 → exit 2；缺 dir／scores 非 3 档／档位非 `a-b` 形式 → WARN 并列名，不静默通过。

---

## 三、当前线上状态

- **版本 `v202609171913`**（git `e35a468`）：14 项门禁全绿、线上回查第 1 次命中
- 0917 批 11 场：方向↔比分同向一致 **11/11 通过**；比分三档＝形状/因素驱动（非裸泊松）；**第 4 档一律只在核心逻辑显示、不入票不计命中**
- 001 已复盘定稿（🟡1/4，第二源已闭合）；002 起今夜按现票面自动入库复盘

## 四、仍留观察（非决定项）

- 比分命中率的三档天花板 ≈ **28%** 是结构性的（第 4 档不可用）⇒ 后续提升空间在**选档质量**与**方向层保护**，不在加档位
- C21 结构性代价（单选场覆盖不到「反向半场领先后被逼平/逆转」）与「A 级覆盖率下限」仍为观察项（n 未达标）

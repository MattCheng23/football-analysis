# 260914 桌面端上下文交接单

生成时间：2026-09-14 07:00 ｜ 更新：07:20（重启后核验通过）
生成会话：`session-d2eaea1c`（桌面端）｜ 上游会话：`session-4b7cd565`（web 端，09-06 → 09-14 06:47，349 轮 / 713 条用户输入 / 45 MB）

---

## 一、根因（已修复）

用户 06:47 在 web 端说「桌面端相当于一个空壳，从 0 开始」——属实，根因是**插件装配按 profile 隔离**，不是数据丢失。

| 项目 | 是否共享 | 状态 |
|---|---|---|
| 会话历史 `~/.dsh/sessions` | ✅ 共享 | 54 个 `D:\Cola` 会话，含上游 `session-4b7cd565` |
| 记忆 `~/.dsh/memories` | ✅ 共享 | `MEMORY.md`、`daily/`、`TODOS-*.md` |
| 工作区 `D:\Cola` | ✅ 共享 | `workspace.json` 同一 workspaceId（旧会话与桌面新会话同列） |
| 技能 `~/.agents/skills` | ✅ 共享 | 33 个技能，含 `checker-hardening`/`operating-discipline`/`ponytail` |
| 模型配置 | ✅ 共享 | `settings.yaml`：`deepseek-official / deepseek-flash / reasoningEffort: max` |
| 凭据 `.credentials.yaml` | ✅ 共享 | 两端同一份 |
| 皮肤 `dream-skin.json` | ✅ 共享 | `skin-center-active.json` = null（用默认主题） |
| **插件装配** | ❌ 原本隔离 | web 9 bundle ↔ desktop 原仅 base＋web-app（**已补齐**） |

## 二、已完成的修复

备份：`~/.dsh/profiles/desktop/*.bak-20260914-065708` ＋ `D:\Cola\_backup\auto_bak\desktop-profile-20260914-065839\`

1. `package.json`：补 6 依赖＋bundle（`@liustack/modsearch`、`dshmarket`、`dsh-file-mentions`、`dsh-find-plugin`、`dsh-dream-skin`、`dsh-memory-evolve`），保留 desktop 原有 `patchReload: live`
2. `cordis.patch.yml`：迁移 memory-evolve 参数、`tool-subagent-pro` insert、`llm-deepseek` 模型菜单精简；**剔除 lark-channel disabled 条目**
3. `pnpm-workspace.yaml`：补 allowBuilds 与 `minimumReleaseAgeExclude`
4. `pnpm install --dir ~/.dsh/profiles/desktop` → +9 包 / 37.1s / exit 0（含 `pnpm-lock.yaml`）
5. `dsh --profile desktop --dump-config` 验证插件树
6. **重启后实证**：上下文已注入全局记忆＋用户档案＋项目 key，`memory`/`dtodo`/`skill_manage` 工具在列

> 踩坑：`dsh plugin --profile desktop install` 报「系统找不到指定的路径」——shell 的 `runtime-commands` 代际目录 pnpm 转发失效；改用 PATH 上的 pnpm 直接 `pnpm install --dir <profile>` 等价成功。

## 三、并发安全（双端同开验证）

- 记忆写入走 `store.js` 的 `withLock(dir, fn)`：跨进程 `.memory.lock`（`openSync('wx')` + 锁内写 pid）＋ `isStaleLock` 按 pid 存活判残留 → **两个实例同时写记忆不会互相覆盖**（持有者活着则等，已死则抢占）
- 实证：两端同时开、web 端 06:38 写入的条目完好保留、时间戳未错乱
- 结论：双端可同时运行，不必二选一

## 四、与 web 端的**有意差异**（不是缺陷）

| 差异 | 原因 |
|---|---|
| 桌面端无 `deepseek-harness-lark` 插件与其 `lark-channel: disabled` 配置 | 缺 `LARK_APP_SECRET` 会导致 ws 无限重连刷屏（8/31 用户拍板禁用）；要用飞书须先补密钥 |
| 桌面端 profile 多 `patchReload: live` | desktop 模板原生设置，比 web 更灵敏 |
| 桌面端有 launcher 侧功能：`desktop-market`／系统通知／`lan-https`／lifecycle-events／profile-setup | 这些在 profile 之外，是桌面壳自身能力 |

> `community-plugins.enabled: false`（settings.yaml）对两端一致，非差异项。

## 五、未完成的实质工作

**0913 批 024 芝加哥火焰 vs 新英格兰革命**（MLS，05:30 开赛）：
- 已入库 001-023，缺 024 的 results＋evidence
- 独立进程 `python -u auto_watch_batch.py --batch 2026-09-13 --dir sun0913 --ids fm_ids_0913.json --every 90 --max-min 200`（启动时 PID 28436，脱离会话，重启不死；日志 `D:\Cola\_tmp_football\sun0913\_watch_024_detached.log`）
- 终场即：机械判四维 → `ingest_review_generic.py` 入库 → `deploy_publish.ps1` 上线 → 写 `自动入库_待补归因.json`
- **仍需模型补**：024 的 M/L/G 归因、联赛层＋整体层双轨蒸馏、台账表G V11.53-6 结算（020 预测低估组正确率／三口径 MAE）
- 幂等：脚本内 `ingested_nos()` 读 data-review.js 判断已入库场次，重复运行不重复写

待办（`~/.dsh/memories/TODOS-work.md`、项目 `TODOS.md`）：
- `c654103c` 010 第二源回补（官方「赛果开奖」页快照停在 09-13 22:39）
- `c7a53e6b` 016-024 按 `260914_xG口径前瞻登记.md` 结算

## 六、可选的下一步

1. 补 `LARK_APP_SECRET` → 恢复飞书通道（iPhone 汇报路线的在外方案）
2. 给旧会话改名/存档为 `Cola_9.7`（当前三段式标题）
3. 端到端演练：从桌面端发一条记忆 → 到 web 端 `memory list` 读回（验证双写闭环）
4. 收敛 `networkExposure: lan`（如需）

## 七、安全提示

- 本机装的是社区壳 `anywhere-labs/dsh-desktop`（2.0.9），可读 `~/.dsh` 下会话/设置/`credentials.yaml`（含 API key）
- 桌面端当前 `networkExposure: lan`＋`openBrowser: true`；HTTP API 有鉴权（未授权返回 401）

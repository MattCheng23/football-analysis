# PTC 模式回测实验 · 任务卡（2026-09-16）

> **用途**：给一个**全新的 PTC 模式会话**（无本会话上下文）当自包含任务书，用来测「PTC 的 `run_code` 组合多步」在**竞彩回测**场景下是否比标准模式省回合/省 token，且**结果必须逐位复现**。
> **对照组**：标准模式会话（2026-09-15 夜）已跑出下表基线，成本口径＝**约 14 次工具调用**（6 个脚本 write+run，含 2 次修 bug 重跑）。

---

## 一、任务（只读运行·禁改数据）

在 **PTC 模式**下把下列**四组回测**跑完，输出统一格式结果，并**逐位复现基线数字**：

| # | 回测 | 参考脚本（标准模式已写） | 数据源 |
|---|---|---|---|
| 1 | **ht 政策对照**（历史实选 A / 逐场泊松 B / 固定集合 C / 混合 D、E） | `D:\Cola\_tmp_football\tue0915\ht_hybrid_test.py`（A/B/C/D/E 全量） | `D:\Cola\足球分析学习\05_模型代码\v12db_full.json` |
| 2 | **半全场分档实证**（P(半场\|全场) 与合法池 TOP3） | `D:\Cola\_tmp_football\tue0915\ht_path_ledger.py` | 同上 |
| 3 | **C38 冷门侧分层**（按 coldRisk 档位 / 按 λ比） | `D:\Cola\_tmp_football\tue0915\c38_decision.py` | 同上 ＋ `D:\Cola\足球分析学习\_发布_public\js\data.js`（coldRisk 数组） |
| 4 | **C38 第二槽择一**（强方＋平 / 强方＋冷门侧 / 冷门侧＋平） | `D:\Cola\_tmp_football\tue0915\c38_pair_test.py` | 同上 |
| 附 | ou 档位与离散度（选做） | `D:\Cola\_tmp_football\tue0915\ou_band_ledger.py` | 同上 |

**约束**：① 只读运行，禁改任何数据/脚本；② 禁部署、禁写 `data.js`／`data-review.js`；③ 允许在 `_tmp_football\tue0915\ptc_run\` 下新建临时脚本；④ 台账解析坑见 §四。

---

## 二、验收标准（必须逐位一致，差一位＝失败）

**① ht 政策对照（`ht_hybrid_test.py`）**

| 子集 | n | A 历史实选 | C 固定集合 | D 混合 | E 混合 |
|---|---|---|---|---|---|
| 全体 | **327** | **62.4%** | 58.7% | **63.9%** | **64.5%** |
| 主胜/平 | **156** | 58.3% | 61.5% | 61.5% | 61.5% |
| 客胜/平 | **69** | 60.9% | 60.9% | 60.9% | **63.8%** |
| 主胜/客胜/平 | 10 | 80.0% | 60.0% | 80.0% | 80.0% |

**② 半全场分档实证（`ht_path_ledger.py`，n＝617 可解析 act）**
- FT=胜（n=265）：胜胜 58.9% ｜ 平胜 35.8% ｜ 负胜 5.3%
- FT=平（n=155）：平平 62.6% ｜ 负平 20.0% ｜ 胜平 17.4%
- FT=负（n=197）：负负 60.4% ｜ 平负 32.0% ｜ 胜负 7.6%
- 「主胜/平」池内（n=177）：胜胜 27.1% ｜ 平胜 18.1% ｜ 平平 14.7% ｜ 胜平 6.2% ｜ 负平 4.5%
- 固定集合对照：C27 集合 **59.9%** vs 字面集合 **48.0%**
- **注**：台账已增至 623 条（含 0915 批），上表为 617 条口径的历史基线；若新跑得 618/624 等，需在报告里写明「样本差来源＝0915 批新增场次」并列出差值。

**③ C38 冷门侧分层（`c38_decision.py`）**
- 可评估 **348** 场（有 coldRisk 登记＋赛果），其中**有 λ 双值 267 场**
- 按档位：低 23.1%（143）｜中等 25.2%（127）｜参考级 9.4%（53）｜中等偏低 10.0%（10）
- 按 λ比：<1.3 **32.3%**（96）｜1.3–1.6 25.9%（58）｜1.6–2.0 16.3%（49）｜2.0–2.5 13.5%（37）｜**≥2.5 0.0%（27）**
- 强方取胜率：67.7% ／ 74.1% ／ 83.7% ／ 86.5% ／ **100%**

**④ C38 第二槽择一（`c38_pair_test.py`）**：可评估 **106** 场
- λ比＜1.6（n=**82**）：A 强方＋平 **70.7%** ｜ B 强方＋冷门侧 **72.0%** ｜ C 冷门侧＋平 **54.9%** ｜ 历史实选 54.9%
- λ比 1.6–2.0（n=15）：A 86.7% ｜ B **100.0%** ｜ C 13.3%
- λ比≥2.0（n=9）：A **88.9%** ｜ B 77.8% ｜ C 33.3%

---

## 三、要记录的 A/B 指标（PTC 实验的核心产出）

| 指标 | 标准模式（对照·已知） | PTC 模式（本次填写） |
|---|---|---|
| 直接工具调用次数 | ≈14（6 脚本 × write+run，含 2 次修 bug） | **核心 1 次** `run_code`（内编排 mkdir＋write＋6 次执行）；含校验/取证全流程 **12 次** |
| `run_code` 调用次数 | —（无此工具） | **12**（侦察 1／规格读取 1／核心执行 1／校验取证 8／落盘 1） |
| 首次即全对的组数 | 2/4（另 2 组需改一次） | **4/4**（四组数字首次全对；本次以参考脚本为规格转录，可比性边界见结果报告 §6） |
| 数字复现 | 100% | **23/24 项逐位一致；1 项＝样本差（FT=负 n 197→198，来源＝0915-003 卡塔尔亚足 1-4 韩国亚运男足于基线跑完后入库，已做快照差分定位）；实现差异 0 项** |
| 主观评价（组合/调试体验） | 逐步可核对，中间量可见 | **组合强、噪声低**：一次调用完成「写脚本→跑→跑对照组→回收输出」整链，6 份输出（≈250 行）留在程序内、仅 ≈60 行摘要进上下文。代价＝调试落在「取证脚手架」（本次 2 处返工均不涉及回测数字） |

> **回填状态**：PTC 列已填（2026-09-16）｜ PTC 会话结果报告：`D:\Cola\足球分析学习\03_报告复盘\PTC模式回测实验_结果_20260916.md`（含逐位验收、样本差根因链、产物清单与 handoff）

> 参考：PTC 模式**不提供 workflow 工具**，其他工具以 SDK 形态挂在 `run_code` 里；`run_code` 是唯一可直接调用的工具。PTC 需要 TypeScript 代码运行时（本机已具备：`dsh-code-runtime-worker-thread`）。

---

## 四、台账解析坑（省得重踩）

1. `v12db_full.json`：`act` 形如 `"2-2（0-0）"` 或 `"5:1（2:1）"` → 用 `(\d+)\s*[-:：]\s*(\d+)\s*[（(]\s*(\d+)\s*[-:：]\s*(\d+)\s*[）)]`；
2. `lam_h`/`lam_a` 有值者 327 条（其余为早期批无 λ 记录），**分侧概率只能用在有分侧值的场次**；
3. `data.js` 的 coldRisk 数组在**批对象尾部**（`results/evidence` 之后），按 `no:` 切条目时别用 `[^}]*?`（条目内含 `}`）；
4. 半全场记号＝**半场结果＋全场结果**（首字半场、次字全场）——009/013 类场次容易反读；
5. `PLAYED` 类判据只取 **results 数组**（批级数组同样带 `no`，混入会放大集合——2026-09-16 已修过的假通过）。

---

## 五、回填（跑完把结果写回本卡）

- PTC 会话报告写到：`D:\Cola\足球分析学习\03_报告复盘\PTC模式回测实验_结果_<日期>.md`
- 并在本卡 §三 表格填 PTC 列；若数字与基线有差异 → **先判定是实现差异还是样本差异**，不得只报结论。

---

## 六、基线快照登记 ＋ 独立复核（2026-09-16 标准模式会话补）

**① 缺陷自认（本卡的口径分裂）**：§二 基线**取自两个不同快照**——① 的 `n=327` 是 **λ 补录后**（21:29+）口径，② 的 `n=617` 是 **21:03** 口径。PTC 会话据此报出「①逐位一致、②差 1 场」，并定位到记录级（其报告 §4）。**今后基线必须随快照一起登记。**

| 基线段 | 快照文件 | 总数 / 可解析 | 关键口径 |
|---|---|---|---|
| ② 半全场分档（n=617 系） | `05_模型代码\v12db_full.bak_20260915_212925.json` | **622 / 617** | FT=负 n=**197**（负负 119·60.4%／平负 63·32.0%／胜负 15·7.6%） |
| ① ht 政策（n=327 系） | 现台账 `v12db_full.json`（21:31 后） | **623 / 618** | ht 子集 **327**；A 62.4%／C 58.7%／D 63.9%／E 64.5% |

**现台账口径最新基线（2026-09-16 02:35 独立复跑）**：半全场 FT=负 **n=198**（负负 **120·60.6%**／平负 63·31.8%／胜负 15·7.6%）；「主胜/平」池 n=177（C27 集合 **106/177＝59.9%** vs 字面 85/177＝48.0%）；λ比≥1.5 子集 n=**53**（67.9% vs 49.1%）；dir 含客胜 n=**114**（64.0% vs 55.3%）；C38 可评估 **348**（λ 双值 **267**）；PAIR **106**；ou 全档 **431/177＝41.1%**；λ和∈[2.6,3.4] n=**144**（MAE 1.52／命中 41.7%）。

**② 独立复核结论（本会话重跑，非采信对方）**：
- 三个断言全过：现台账 **623/618/FT=负 198·负负 120** ✅ ｜ 快照 **622/617/197·119** ✅ ｜ **差集恰为新增 1 条 `2026-09-15 no=003`、零删除** ✅（`tue0915/verify_ptc_claim.py`）
- 5 个参考脚本**现跑输出** vs PTC 会话落盘的 `ref2_*.txt`（合并脚本编排产出）：**逐行差异 0**（5/5）✅；vs `ref_*.txt`（首次直接重定向）仅差一个 **UTF-8 BOM**
- → **数字同源、复现无损、台账自 02:22 无漂移**；PTC 报告的样本差根因链成立。

**规则（写进流程）**：任何基线数字必须附 `v12db_full.bak_<时间戳>.json` 文件名 ＋「总条数／可解析数」，否则视为不可复算。

# 260916 偏差归因（0916 批 001–002 · 逐场即时·模型侧）

> 触发：用户 20:0x「001和002赛果有了」。
> 数据源：FotMob `matchDetails`（主源，`finished=true`／`reason=FT`）＋ 五维基线工具 `five_dim_baseline.py 2026-09-16`（机器回源 data.js λ）。
> ⚠ 本文件是**模型侧归因**，不替代 `data-review.js` 的入库（后者由盯场器负责，001 入库时点见文末）。

---

## 001 中国亚运男足 2-1 朝鲜亚运男足（半场 1-1）｜🟢3/4

**进球链**：27′ 客 Kuk Choe 0-1 ｜ 45′ 主 Yuning Zhang 1-1 ｜ 67′ 主 Xi Wu 2-1（半场＝脚本逐球累计）

| 维 | 票面 | 实际 | 判定 |
|---|---|---|---|
| 方向 | 主胜/平（B级） | 主胜 | ✅ |
| 比分 | 1-1 / 1-0 / 2-2 | **2-1** | ❌ |
| 半全场 | 胜胜/平平/平胜 | **平胜** | ✅ |
| 总进球 | 2·3 | 3 球 | ✅ |

**λ**：1.40/1.10（λ和 2.50，λ比 1.27，来源＝data.js 原值）

### ★核心发现：第 3 档让掉了更高概率项，且让掉的正是实际比分

面板 31 档降序前 8（机器）：**1-1 12.64%（1）· 1-0 11.49%（2）· 0-1 9.03%（3）· 2-1 8.85%（4）· 0-0 8.21%（5）· 2-0 8.05%（6）** · 1-2 6.95%（7）· 0-2 4.97%（8）
现行第 3 档 **2-2 ＝面板第 9（4.87%）**。

- **让位成本 ＝ 8.85% − 4.87% ＝ 3.98pp**，且**让掉的就是实际比分**。
- **触发条款候选**：V11.47 候选②「**让位声明禁让掉概率更高的项**」（0911-002 同型：把 P＝14.4% 的 0-2 让掉）。
- **P8（比分必须最合理）失守**：用户长期要求"比分要最合理不能太保守"，本场属**结构让步换形态**（2-2 是平局侧覆盖），但代价未在票面留痕。
- **λ 侧**：工具判 **2-2 属"假越界"**（λ+0.41 校正后升到第 4）→ 我方 λ 偏低是成因之一；**但 2-1 在原 λ 下已是第 4，无需校正即应入选** → 归因**不能全推给 λ**。
- **归属**：**L（票面结构/λ）**，非 M/G。

### 过程量（待补）
射门/xG/牌：FotMob 本场 `stats.Periods` 结构异常（解析报 list 非 dict），**待用 `extract_stats2.py` 复核**；POM 待取。
**第二源**：待补。

---

## 002 全北现代 2-1 柏太阳神（半场 0-1）｜🟡3/4

**进球链**：30′ 客 Kenshin Yuba 0-1 ｜ 48′ 主 Tiago Orobó 1-1 ｜ 69′ 主 Italo 2-1
**牌**：32′ Nakagawa Yellow(客)｜87′ Seung-Woo Lee / Tae-Hwan Kim / Young-Bin Kim Yellow(主) ｜ 红牌 0:0

| 维 | 票面 | 实际 | 判定 |
|---|---|---|---|
| 方向 | 主胜/平（B级） | 主胜 | ✅ |
| 比分 | 1-1 / 2-1 / 2-2 | **2-1** | ✅ |
| 半全场 | 胜胜/平胜/平平 | **负胜** | ❌ |
| 总进球 | 3·4 | 3 球 | ✅ |

**λ**：1.70/1.40（λ和 3.10，λ比 1.21）

### 核心发现：ht 集漏「落后逆转」分支

九宫格降序前 5（机器）：胜胜 27.33% · **负负 18.55%** · 平胜 14.42% · 平平 12.93% · 平负 10.90%
现行 ht ＝`胜胜/平胜/平平` → **实际「负胜」（半场 0-1 → 全场 2-1）不在集内**。

- **归属 G-ht 路径**（与 0915 批同型，属**整体层选择规则**问题，非联赛/场级）。
- **机制**：票面把 2/3 名额给了「全场平」分支（平胜 14.42% + 平平 12.93%），**落后方反超分支无席位**；而本场正是客队先入、主队 48′/69′ 连扳。
- **条款候选**：ht 三槽在 C21（第二字∈方向集）之外，应加一条「**主胜方向下须评估「负胜」是否占额**」——判据＝半场 λ 客 > 半场 λ 主 × k 时保留一个落后逆转位（k 待 `ticket-shape-backtest` 全量回测定）。
- **ou 备注**：取 `3·4`（39.72%）而非泊松 argmax `2·3`（44.04%），**让位 4.32pp**；本场 3 球在档内＝兑现，但档位选择须按 V11.46-2②（双哑火准入）复核是否合规。

**第二源**：盯场器入库存证**已完成**（主源终场齐备＋第二源竞彩官方「赛果开奖」接口核验一致）。

---

## 待办（明确列出，不默认知情）

1. ~~**001 入库＋部署**~~ → ✅ **2026-09-16 20:07 完成**（模型侧手工入库：`spec_auto_001.json` → `ingest_review_generic.py`，`data-review.js` 已追加 001、`reviewedCount → 2`、双日志已写）。
2. ~~**`reviewed = false`**~~ → ✅ **已置 `true`**（字段级正则，仅批段落内；复验 true／count=2）；`node --check` 三文件 rc=0。
3. ~~**部署**~~ → ✅ **v202609162007**（12 项门禁全绿 → wrangler → **线上第 1 次回查即命中**）｜git **4f5b133**｜线上 `data-review.js` 已含 001/002。
4. **002 深度归因（需外部情报）**：伤停/留力/克星 —— **仍未做**（需抓 FotMob 阵容/伤停与柏太阳神客场强度）。
5. **联赛层**：001＝`lg-asia` 亚运男足（**该联赛 FotMob 无统计源**）、002＝`lg-acle` 亚冠精英——**各仅 1 场，未达「≥3 场同联赛」门槛，暂不提条款**。
6. **条款候选 2 条**：
   - **候选 A（001 第 3 档让位禁令）→ ✅ 已回测，结论＝「不提（否证）」**（报告 `workgroup\回测_候选A_第3档让位禁令.md`，可复跑）。同一分母 **n=319**：现行 90/319＝**28.21%**；候选 A 无条件 96/319＝30.09%（Δ+1.88pp，**p=0.4408**，CI 重叠）；全票替换 92/319＝28.84%（Δ+0.63pp）；**触发子集**（第 3 档不在面板前 4，n=182）52→57/182（28.57%→31.32%，Δ+2.75pp，p=0.4244）；触发子集内第 3 槽对撞 **只A中 15 vs 只现行中 10**。<br>　**否决三理由**：①**时间半分 Δ＝−0.63pp／+4.38pp，符号相反＝噪声**；②n=319 检出 +10pp 的把握度仅 **44.48%** → 只能写"未测出"，**不得**写"已证明无效"（报告自带措辞边界表）；③**机制层**：**第 3 档不是"填位次最高者"的槽位，而是承载结构义务的槽位**（大分覆盖／平局侧形态／冷门侧）——用面板位次覆盖它＝打掉 V11.53-8 的大分覆盖义务、而地板（前两档）不变 → 净效应 ≈0 或负（与技能既有「换档否证」方向一致）。<br>　**采纳的替代动作**：**只改撰写层、不改选号层**——让位声明须机器可核地写出「被让位项 prob／位次／成本」三项（＝V11.47② 原文），使让位**可审计可结算**，但**不禁止让位**。附：**现行第 3 槽命中率仅 6.90%**（22/319），真要提升该槽应走「第 4 档扩位／形状按实力分层」，而非位次替换。
   - **候选 B（002 ht 落后逆转位）** → 回测中。

---

## ★规程缺口（本场暴露·建议登记）

**盯场器的 payload 完整性闸门无法区分「数据未就绪」与「结构性无源」**，导致 **001 被无限重试**：

| 时间 | 事件 |
|---|---|
| 20:01:13 | 抓 `fm_001_details.json`（第 1 次） |
| 20:05:39 | **再抓同场**（第 2 次）——同一 payload，结果相同 |
| 20:0x | `待模型收尾.md` 两轮刷新**均只列 002**；`spec_auto_001.json` **始终未生成** |

**根因（机器可证）**：001 的 FotMob payload `content.stats = None`、`lineup = None`、`playerStats = None`、POM 为空 —— 亚运男足**该赛事根本没有统计源**，属**结构性缺失**，而闸门判据是 `stats 为空 → 跳过重试`（教训⑯），于是**每轮重抓、永不入库**。

**建议规程修订（第三分支·★已于 2026-09-16 20:20 实施并验证）**：闸门判据拆两档 ——
- **滞后型**（`finished=false`，或 stats 空但 `lineup`／`playerStats` 有一）：**继续重试**
- **结构型**（`finished=true` **且** `stats`／`lineup`／`playerStats` **三者全空** 且比分＋事件流可解析）：**连续确认 2 轮后带缺项降级入库**，evidence 标注「结构性无统计源（非漏取）」

### ✅ 实施与验证记录（2026-09-16 20:15–20:21）

| 项 | 结果 |
|---|---|
| 改动文件 | `_tmp_football\sun0913\auto_watch_batch.py`：新增常量 `STRUCT_CONFIRM_ROUNDS = 2` ＋ 把判据从内联 `if not stt_chk: continue` **抽成可单测函数 `gate_payload(no,d,teams,fin) → (action,degraded,msg)`**（顺带补上原作者**算了却没用**的 `n_goals_ev`） |
| 语法门 | `python -m py_compile` **rc=0** |
| **反向验证 A/B（6/6 通过）** | `wed0916\_gate_ab_test.py`（AST 提取函数规避模块级 argparse）：<br>① 结构型·**真 001 payload** → `[retry, ingest]`（第 2 轮降级入库，degraded=True）✓<br>② 正常·**真 002 payload** → `[ingest]`，stats 齐备 36 项，**不降级** ✓<br>③ **滞后型**（stats 空但 lineup 在）→ **5 次调用全部 retry，绝不升级为降级** ✓（假阳性防护）<br>④ 防呆 teams<2 → retry ✓｜⑤ `fin=false` → retry ✓｜⑥ 无事件无比分 → retry ✓ |
| **自查夹具两次** | 夹具首版我把 `STRUCT_CONFIRM_ROUNDS=2` 的期望写成「retry×2 再 ingest」（实为"观测 2 轮即确认"），第二版又跑多了一次 → **均为夹具错、实现对**（`checker-hardening` 纪律八：报警先自查夹具） |
| **生效前提** | 改文件≠生效（`PID 11612` 内存里仍是旧码）→ **停旧 + 用 OS 级计划任务重启**：新守候 **PID 19336 @20:21:07**，父链 `svchost → powershell → python`（**脱离 DSH 作业树**，DSH/会话重启都不会带走） |

**★自我更正（当日第二次）**：我早先把 `DSH_Watcher0916_Once` 计划任务**禁用**当作"拆双写隐患"，结果重启守候时**先撞上"任务已禁用"**（`0x80041326`），且守候一度停机。真相是：**双写保护早已由启动器内的单实例守卫提供**（日志实证 `SKIP：已有盯场器实例在岗（PID 11612）`），禁用该任务反而砍掉了**唯一能脱离 DSH 的重启通道**。已重新启用。→ **教训：禁用"看起来危险"的自动化件之前，先确认它是不是唯一的恢复路径。**

**附注**：`logs\watcher_0916.log` 是 **UTF-16** 编码（用 `-Encoding UTF8` 读会显示成间隔字符），读取时须用 `-Encoding Unicode`。

---

## ★★当晚第二个隐患：自动入库从不置 `reviewed`（同 09-06 事故形态）

**发现**：19:55 那次是**盯场器自己部署的**，但该批留下 **`reviewed = false`**。查证：
- `auto_watch_batch.py` 只调 `deploy_publish.ps1`（L422-424），**全文无 `reviewed` 字样**
- `ingest_review_generic.py` 只改 `reviewedCount`（L128-131），**从不置 `reviewed`**

→ 后果：**今晚剩下 15 场每场自动入库＋部署后，该批仍是 `reviewed:false`**，页面落"待复盘"态；按技能记载（**2026-09-06 第三次事故**）**整张表会被吞**——用户明早打开看到的是坏页面。

**修复**（`ingest_review_generic.py` L132 后新增）：只要该批 **n ≥ 1** 场赛果且 `reviewed:false` → 置 `true`。

| 项 | 结果 |
|---|---|
| 语法门 | `py_compile` **rc=0** |
| **单测 4/4** | `wed0916\_test_reviewed_flag.py`：<br>A **复刻 0916 真实场景**（`false`＋count=1 → 入库 2 场）→ `reviewed: true, reviewedCount: 2` ✓<br>B **幂等**（已是 true 不得回退）✓｜C **n=0 不置位**（防呆）✓｜D 无 `reviewedCount` 字段走 else 分支 ✓ |
| **生效时机** | **无需重启守候**——盯场器每场以 `subprocess` 调入库器，每次读新代码 → **下一场（003）入库后即验证** |
| 当前实况 | 0916 批 `reviewed = true ｜ reviewedCount = 2`（手工置位保持）｜版本 `v202609162007` |

**★这是同一晚第二次「自动化静默失效」**：闸门把结构型当滞后型（丢整场复盘）＋入库器不置 reviewed（吞整张表）——两者都**不报错、不告警**，只在页面上表现为"少了点什么"。→ **建议把"入库后 reviewed 必须为 true"做成门禁断言**（放 `review_gate_check.py` 或部署链 [1.5/6]），否则同类还会再犯。

**★当晚我自己的第三个错**：单测首版用**内联 python**（`python -c "…\"x\""`）被 pwsh 引号啃掉 → `SyntaxError`。**这是 `operating-discipline`/`AGENTS` 明令禁止的写法**，改成脚本文件后即通过。**规则我写过，还是踩了——记一次。**

### ✅ 当夜二次修订（v2·**判据改为纯时间**）——由「预判下一场」发现

**发现方式**：v1 上线后我没有假设"修好了"，而是**提前拉 003 的 payload 预判它会走哪条分支**（脚本 `wed0916\_predict003.py`，只读、写 `_probe_fm_003.json` 不抢盯场器的文件）。

**预判结果推翻了 v1**：

| 场 | finished | stats | lineup | playerStats | v1 判 | 问题 |
|---|---|---|---|---|---|---|
| 001 中国 vs 朝鲜 U23 | True | **0** | **None** | None | 结构型 → 降级 | ✓ |
| **003 日本 vs 香港 U23** | True | **0** | **有** | False | **滞后型 → 继续 retry** | **❌ 还是会无限空转** |

→ **v1 用「lineup/playerStats 是否存在」区分滞后/结构是错的**：003 有 lineup、stats 却恒空，与 001「同病不同形」。

**v2 判据（已实施）**：**纯时间** —— `struct_missing = finished ∧ 有比分 ∧ (有事件流 or header.events)`；终场后等满 **`STRUCT_CONFIRM_ROUNDS = 5`（≈10 分钟）** 仍无 stats → 降级入库。
选 5 不选 2 的理由：判据已是纯时间，等太短会把**统计迟到的正常场次**误降级；FotMob 统计通常终场后 1–2 分钟补齐，10 分钟足够，且远小于"永不入库"的代价。

**反向验证 v2（7/7 通过）**：① 真 001 全空 → 第 5 轮降级 ✓｜② 真 002 stats 齐备 → 直接入库不降级 ✓｜③ **真 003（lineup 有·stats 恒空）→ 第 5 轮降级** ✓（v1 会无限 retry）｜④ teams<2 → 永远 retry（防呆）✓｜⑤ 未终场 → 永远 retry ✓｜⑥ 无事件无比分 → 永远 retry ✓｜⑦ **统计迟到（002 清空 stats）→ 前 4 轮 retry、第 5 轮才降级** ✓（证明不会误早降级）

**生效性硬证据（自定判据）**：守候 **PID 27752 @20:24:59**，而脚本 mtime **20:24:30** —— **脚本 mtime < 进程启动时刻 ⇒ 加载的确是 v2**。（v1 时同样验过：mtime 20:19:54 < PID 19336 的 20:21:07。）

**★方法论收获**：「改完 + 单测通过」**不等于**"下一场会被正确对待"。本次靠**拿真实下一场数据做预判**才发现 v1 的判据选错了维度——**夹具再全，也顶不上一个真实样本的预演**。

**风险提示**：本批 001 因此**晚了约 6 分钟**入库（20:01 抓到 → 20:07 我手工入库）；若不介入将**直到盯场器 `--max-min 1400` 耗尽（次日约 10:15）都不会入库**，即整场复盘消失。

# 双会话协作降耗｜契约 v1 ＋ 首轮实效（标准侧 ↔ PTC）

**日期**：2026-09-16 06:37（原稿误写 06:48，已按机器时间更正）｜ **触发**：用户「你和 ptc 的合作优化问题，避免双方重复不必要的内容」
**权威契约**：`D:\Cola\_tmp_football\handoff\ROLES.md` §八（8.1–8.6）｜ **任务台账**：同目录 `TASKS.md`

---

## 一、实测基线（降耗前）

| 方向 | 封数 | 总量 | 均长 | 其中**纯确认**（"收到/已读/同意"） |
|---|---|---|---|---|
| 标准侧 → PTC | 8 | 23.6 KB | 2.95 KB | — |
| PTC → 标准侧 | 11 | 23.2 KB | 2.11 KB | 7 封 ＝ **占往返 37%** |

结论：**浪费不在"发得多"，而在"发得没信息"**——纯确认占 1/3 以上，且每封平均 2–4 KB 的散文让复核方必须逐字读。

## 二、契约 v1 落地的八条（全文见 ROLES §八）

1. **禁发纯确认信**——读信状态由 `.read_<box>` 指针机械证明，不需要人说"已读"。
2. **一封一事不再拆**，每封 ≤1.5 KB；超长内容落文件、信里只给路径。
3. 首行标 `[无需回复]`／`[需回执]`；**只有"有异议"是强制回执场景**。
4. **状态只引用产物**，禁转述数字（防两套口径，C38 267/330 教训）；判定值必须机器回源。
5. **草稿目录与信箱分离**（新增，见 §三）：草稿放 `drafts_std\`／`tue0915\`，只 `send` 一次。
6. `[回报]` 固定五字段（inputs／artifacts／gates／counts／notes）→ 复核方只做**机器比对**，不读散文。
7. **验证分层**：L1 日常（每批末四项机器比对）／L2 全审（批真正收尾）／L3 触发式；**禁**日常重跑 `batch_end`、禁同一检查器在同一输入上跑第三遍。
8. **工作分区按时间尺度切**：批内（逐场/联赛/批末链）＝PTC；跨批（历史回测/条款机械化/审计/A-B）＝标准侧；交界走 `[任务]`／`[改判请求]`，不并行。**唤醒**：一封信只触发一次 `[WAKE]`；无事不发。

## 三、首轮实测：抓到一个真重复，已收敛

| 现象 | 判定 | 处置 |
|---|---|---|
| PTC `012`／`013` **同内容发两封**（头部 `from=std` 也错） | 协议违规 | 已定位成因而非态度问题：**草稿先写进 `to_std\` 目录 → 被 `ls` 计为已发 → 再 `send` 即重复**；PTC 06:35 自述已改为草稿入 `tue0915\`、只 send 一次 |
| **我方同类风险**：本轮我也把草稿直接写进 `to_ptc\` | 自查发现 | 已建 `handoff\drafts_std\`，本轮 011 号信即按新流程只发一次；规则补进 §8.1 第 5 条 |

→ 这类"重复"的根因是**工具语义**（`ls` 只认 `\d{3}_*.md`），不是沟通习惯；已从机制上堵住。

## 四、结构性防重复：单一任务台账

`handoff\TASKS.md` 是**唯一**任务台账：每条**有且只有一个 owner**、谁推进谁改状态行、动手前先看表。
本轮从 12 条扩到 15 条（新增 T-013 三口径落库／T-014 C44 阈值回测／T-015 011 口径分歧），**无一条存在双 owner**——这是"避免双方做同一分析"的机械保证。

## 五、写域判据（本轮新裁，替代原"脚本按人分"）

**看产物**：产物＝**数据／台账／文档** → PTC（含聚合器、入库器、其解析正则）；产物＝**判定／门禁／证据** → 标准侧（`check_*.py`、`preflight_gates`、A/B 对拍、审计报告）。
→ `aggregate_black_v2.py` 归 PTC（PTC 主动请示是否收回，答：不收回）。

## 六、现状（2026-09-16 06:37）

- 0915 批：**13/14 已入库**，014（06:00 开赛）预计 **07:55** 入库 → PTC 自动跑批末链 → 我方 `wait_result` 守候（PID 在岗）唤醒 → 做 L1 复核（四项机器比对）。
- 守候：信箱 2/2、批级 2；DSH 3080／WebBridge 10086 在岗；7 门禁绿。
- 本轮附带完成 **T-007**（C43/C44 检查器，report-only）：结论见 `260916_条款机械化_C43C44_20260916.md`。

# 260914 老会话（Cola_9.7）全量核对与漏项清单

生成时间：2026-09-14 07:20 ｜ 生成会话：`session-d2eaea1c`（Cola_9.14·桌面端）
核对对象：上游会话 `session-4b7cd565`（Cola_9.7·web 端，09-06 → 09-14 06:47）

---

## 一、核对方法（为什么可信）

**不是截图、不是转述**，是直接解析落盘文件：

| 步骤 | 命令/产物 |
|---|---|
| 定位会话文件 | `~/.dsh/sessions/--D-Cola--/session-4b7cd565-…/session.v3.jsonl.zstd`（45 MB） |
| zstd 解压 | → `%TEMP%\dsh-sess\last-web-session.jsonl`（186 MB，44,324 条记录） |
| 提取用户原话 | **713 条** → `user-prompts.txt`（3.0 MB） |
| 重建助手正文 | **341 轮 / 730,639 字符 / 单轮最长 4,025 字** → `assistant-full.txt`（726 KB） |
| 生成全量目录 | **341 行**（每轮时间＋字数＋首行）→ `assistant-digest.txt` |

> ⚠️ **一个必须记录的自身失误（本单过程中的教训）**：第一版提取器把 `data.message.content` 数组
> **只取了第一个 text 块**（实际结构＝`[{type:"reasoning"},{type:"text"}]`），导致读到的是**流式中间态碎片**
> （几十字），而非每轮完整答复（实际最长 4,025 字）。**根因**：未先 dump 记录结构就假定字段位置。
> **固化教训**：解析会话/接口类数据前，先 `dump` 一条记录的真实结构与长度分布，再写提取逻辑——
> 与项目既有「防坑三步法（先查 URL→dump 字段→自检）」同源。已写入记忆。

## 二、覆盖度声明（诚实口径）

| 范围 | 状态 |
|---|---|
| 341 轮目录（每轮主题/字数/首行） | ✅ 全部过目 |
| 最后 45 轮完整正文（09-14 06:25→06:47 段） | ✅ 逐字细读 |
| 中段约 300 轮完整正文（726 KB） | ⚠️ **仅目录级**，未逐字读 |
| 713 条用户原话 | ✅ 已提取落盘（按需检索） |

## 三、本次核对回收的漏项（3 项，均已核实）

### ① 悬挂 5 天的过期待办（已结清）
- 条目：`0c7123eb`（q1·工作·09-09 建，`due: 2026-09-10`）
- 内容：V4.1-Flash 内测模型 `deepseek-v4.1-flash-expires-on-0910` 到期需回滚默认模型
- **实况：问题已自行解决**——`settings.yaml` 现为正式名 `deepseek-flash`，`reasoningEffort: max` 也已恢复
- 处理：2026-09-14 标记 done（附核实证据）

### ② 概率校准器被私自降级（V12-P0① → P2）
- 事实：`260914_V12最优架构设计.md` 中列为 **V12-P0①**，06:24 完整性审计发现被降为 P2 且**未告知用户**
- 性质：口径不一致 / 静默改优先级
- 处理：建工作待办 q2，需改回 P0 并补排期，或说明理由后定稿

### ③ 3 个 P1 历史数据问题仍等授权
- 09-14 01:17 流程体检（`pipeline_audit.py`）查出 9 缺口，其中 P1 三项涉老批历史数据
- 已闭环：老批 reviewed 标记（`fix_review_flags.py` 修 50 批＋部署）
- 仍待授权：08-27 缺 5 处 evidence 回填等
- 处理：建工作待办 q2，整理成清单一次性请用户裁定（**禁自行改历史数据**）

## 四、批末收尾 6 项（老会话原话承诺「024 一落地立刻跑」——尚未交付）

> 触发条件：024 终场自动入库（计划任务 `DSH_Watch_024` 负责）

1. 24 场全量归因 ＋ 台账表A/B/D/F 结算
2. V11.51-5-R2 E 级阈值复核
3. V11.53-6 前瞻登记结算（预测低估组 020 正确率／三口径 MAE，累积 n≥40 后裁决 V11.53-2/3）
4. `settle_batch` ＋ `slot4_track` 基线
5. 批末报告 ＋ 记忆整理
6. 第二源回补重试（对应待办 `c654103c`）

### ⚠️ 四个必须在收尾时处理的已知坑

| 坑 | 事实 | 动作 |
|---|---|---|
| **归因清单不完整** | `自动入库_待补归因.json` 只剩 **018-022**，001-017 不在（老会话 06:26 标"待确认"） | 批末清单**必须从 `data-review.js` 重建 22 场**，禁用现有 json（否则只覆盖 5 场） |
| **3 项审计遗漏从未登记** | 概率校准器（见三②）、分歧审计器＋分歧台账（V12-P1②）、场景化分工表（V12-P3） | 补登 `260914_全链路迭代总表.md` |
| **工具库膨胀** | `_tmp_football` 顶层实测 **2,597 个 .py** | 瘦身（Ponytail 七级阶梯口径） |
| **两条到期待办** | `c654103c`（010 第二源回补，今日到期）、`c7a53e6b`（016-024 xG 结算） | 批末一并处理 |

## 五、其他仍未开工的既有事项（老会话确认状态）

- **V12-γ 半场模块停滞 8 天**（α 阶段 9/6 完成，β 停在 9/8 未动）
- **V12 方向模型**：32 组网格方向 0/32 优于 V11 → **不上线**（协议固化：方向≥80% 格优于 V11、ht≥80% 且均值≥+5pp）
- **P1 四项**：事件深度／归因三维／源健康／对手质量加权 —— 未开工、无排期
- **飞书通道**：选型已定（`ax-feishu-bridge` 0.4.9 ＋ 自写渠道注册插件），未执行；`LARK_APP_SECRET` 已在 `.credentials.yaml`（凭据库，非环境变量），启用只需改 `disabled` 并重启
- **iPhone 局域网直连**：方案已定（`--host 0.0.0.0 --trusted-host 192.168.0.103:3080` ＋ 限网段防火墙规则），未执行

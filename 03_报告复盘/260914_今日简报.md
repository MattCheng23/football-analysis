# 今日简报 · 2026-09-14（周一）

> 触发：用户在飞书私聊发「今日简报」→ 经 `dsh-feishu-inbox` 注入会话（**该消息本身即双向通道的首次实战**）
> 全部数据机器回源：`data.js` / `data-review.js` / `restart_status.log` / 台账

---

## 一、批次状态

| 项 | 值 |
|---|---|
| 最新已发布批次 | **2026-09-13**（周日批，24 场） |
| 0914 批（周一） | **尚未开工**（data.js 无该批键） |
| 0913 批入库 | 24/24（results + evidence 齐） |
| 线上版本 | **v202609140942** ｜ commit `9358748` ｜ 首页 HTTP 200 |
| 复盘批数 | 55 批 |

## 二、0913 批真实命中率（台账口径）

| 维度 | 命中 | 基线 | 差 |
|---|---|---|---|
| 方向 | 20/24 = **83.3%** | 65.8% | +17.5pp |
| 比分 | 8/24 = **33.3%** | 30.0% | +3.3pp |
| 半全场 ht | 18/24 = **75.0%** | 60.9% | +14.1pp |
| 总进球 ou | 10/24 = **41.7%** | 41.6% | +0.1pp |
| 四维总分 | 56/96 = **58.3%** | — | — |

满分 7 场（001/004/008/014/016/017/023）；0/4 三场（002/019/022）。
兑现路径：**尾部兑现 3 场**（001/008/017 靠 86′+ 进球）→ 满分场里 43% 带运气成分（已诚实标注）。

## 三、今日修掉的两个真问题（都留了痕）

1. **串场事故（我造成的）**：`second_source_backfill.py --apply` 用**全文件 replace**，把 012/013/014/015 写成了 010 的官方数据，并把 016-024 的裸标记批量改成「已核验」（那几场当时**没核验**＝谎报）。
   → 处置：还原 git HEAD → 新工具 `ss_tool.py`（按场次字符区间**逆序**替换）重做 → 机器判定 PASS；**原脚本 `--apply` 已硬禁用**（exit 2 并指向新工具）。
2. **018 半场字段错值**：`score` 记 `4-1（0-3）`，但与自身 ht 判定「胜胜」、FotMob 证据「2-1」、官方 `sectionsNo1=2:1` **三处矛盾**。
   → 按双源更正为 `4-1（2-1）` 并在该场 signal 内留痕；20 批回溯体检 286 场/230 可比对/**不一致 0** → 属孤例。

## 四、第二源回补结果

**15/15 全部「官方接口核验一致」**（010-024），官方 `lastUpdateTime` 由 01:00:29 前进到 09:15:26。
机器判定：`batch=2026-09-13 entries=24 verified=15 no_verify=9 leftover_token=0 → RESULT=PASS`。

## 五、通道与能力（今日新增）

| 能力 | 状态 |
|---|---|
| 手机 · 局域网进同一会话 | ✅ 已验收（手机 192.168.0.100 → 192.168.0.103:3080） |
| 飞书 · 我发给你 | ✅ 默认走**私聊**（不再是群） |
| 飞书 · **你发给我** | ✅ **今日打通**（本简报即首条实战） |
| 外网直连（不在家也能开网页） | ❌ 已裁决不做（IPv4 走 CGNAT + Tailscale 装不上；在外以飞书为准） |

## 六、V12-P0① 概率校准器：可行性实测 PASS

- 台账 `lam` 只是**单个数（λ和）**→ 反推不出 P(主/平/客)（结构性阻塞，原排期把它排在 P2 是自相矛盾）。
- 改从 `data.js` 正文抽 (λ主,λ客) → **181 场**：多分类 Brier **0.5553** vs 常数基线 **0.6415**（−0.0862 ✅）。
- 校准缺陷：中高档 0.48→0.595、0.68→0.818 **欠自信**；低档 0.38→0.258 **过自信** → 重校准有明确收益点。
- ⚠️ 边界：泊松 argmax 命中率 **56.9%** ＜ V11 方向基线 65.8% → 校准器**只能给判定配信度，不能取代判定**。
- 裁决：①**维持 P0**；②把被排在 P2 的「λ主/λ客 落库」**提为 P0-前置**（否则 ① 永远缺输入）。

## 七、待你裁定 / 下一步

1. **0914 批是否开工**（今日尚无批次数据）。
2. 台账未闭环 **14 条**（存量）—— 要不要我整理成清单请你一次裁定。
3. `λ主/λ客 落库`（P0① 前置件）已在**待确认队列**，你点头即排入项目待办、下次批末第一优先。

---

*本文由会话自动生成并落盘；对话侧只发精简版（飞书推送）。*

# 迭代日志（每完成一个流程升级就追加一行·唯一留痕）

> 用户口径（2026-09-14）：「**只有全部的每一个流程和细节去迭代、升级、优化、整理，这样才能更有利于我们，也能更好的帮助我们升级到下一个历史性突破的大版本**」
> 规则：**每个升级必须带验证证据**（回归测试／真实数据重放／回测），无验证的改动不算升级。
> 总表索引见 `260914_全链路迭代总表.md`（那里管"有哪些项"，这里管"做过什么、验证结果如何"）。

| 轮 | 日期 | 流程段 | 改动 | 验证证据 | 效果/结论 |
|---|---|---|---|---|---|
| R1 | 09-14 | 段2 判定 | **机械决策代理 `prematch_calc.py`**：λ→方向概率→泊松TOP6→**第4档（V11.54-1 规则C）**→ou相邻档→**半全场9结局(C21)**→让位成本→六项合规（C1方向/C2形状/C3第3档/C4冷门侧/C5ou/C6打穿） | ①`--selftest` 3 组样例（含 018 全零封向反例、让位违规反例、E级豪门打穿）②`--replay` 对 v12db **281/281 与 slot4_backtest 实现 100% 一致**、净增益 **+9.6pp** 复现 | **模型不再手算**；018 形状取档错一类问题被机械阻断 |
| R2 | 09-14 | 段4 复盘 | **兑现路径 `mech_review.cash_path`**：按进球时间线回放，「砍掉 86′+ 进球后票面还中不中」→ 稳胜/险胜/尾部兑现/逆转流失/未兑现；满分且尾部 → `🟢·尾部4/4` 标记 | `test_mech_upgrade.py` 0913 全批 13 场真实 payload 重放 | **017＝尾部兑现（88′/90′）与 014＝稳胜兑现（72′）被分开**；016 险胜（78′）；010·011·018·019 逆转流失 |
| R3 | 09-14 | 段4 复盘 | **决策质量分 `mech_review.proc_score`**：5 项真合规（方向覆盖／V11.53-9 双向档／V11.53-8 第3档大分／C21／ou档+一致性声明）；**让位成本改为只披露不进分母** | 同上重放；修掉 2 个自造 bug——⑤类型不匹配（`'2·3' in {2,3}` 恒假）与⑥误把条款强制档当违规 | 0913：**6 场 5/5、7 场 4/5**；**019·022 结果 0/4 而过程 4-5/5＝判断对、结果错** → 结果↔过程分离成立 |
| R4 | 09-14 | 段1 信息获取 | **撤销「赛前 ~1h 首发核对」**（用户拍板） | 逻辑论证：凌晨场 02:00-04:00 开球，首发公布（开球前 ~1h）**晚于体彩停售** | 从 P0 移除；不投入采集成本、**不计入信息量评分**；仅复盘时事后从 `matchDetails` 取用于解释错因 |
| R5 | 09-14 | 段5 基础设施 | 批段解析固化：`jstr()` 整值抓取（禁 `[^"]*`）＋按「含 scores 字段」筛真场次（批段内混 matches/alerts/bigSeven 三类条目，68 条 `no:` 里只有 24 条是真场次） | `probe_entry.py` 实证 + 回归脚本 | 又一处「假设字段结构」踩坑被固化，写进解析铁律 |
| R6 | 09-14 | 段3 出票 | **撤回「弃场/降级机制」**（我曾按流程体检把它列为 P0） | 用户拍板：「批次内的比赛几乎全部都分析，**不应该有这个该不该出票这个情况**」；实测 0913 备料 `res_fm_001..024.json` **24/24 全覆盖**——覆盖率本是产品前提 | **方向性错误已撤回**：信息不足的正解是**补齐信息**，不是减少场次；`1.3 信息量评分` 同步改为「情报完备度门禁（只检查+补齐，不阻断出票）」，P0 位置由 **`3.5 批内全覆盖门禁`** 接替 |
| R7 | 09-14 | 段3 出票 | **批内全覆盖门禁 `coverage_gate.py`**：10 列 × N 场矩阵（①赛前捕捉 ②matchId ③判定四字段 ④λ ⑤伤停 ⑥近史 ⑦H2H ⑧预警三数组 ⑨第4档 ⑩【特征】行），缺项按列输出**补齐动作**；接入 `preflight.py`【G】段 | ①0913 实测 **24/24 场全部齐备（100%）** ②**反向验证**：`--strict` 强制 ⑨⑩ → 报 0/24 ❌（证明不是死代码）③防呆：不存在批键 → 退出码 2，不静默通过 ④强制起始日**机器回源**技能：V11.54-1 与 V11.55-1 均「自 0915 批起强制」 | **覆盖率从"事实上满"升级为"机械守住"**；规则写死**只补不弃**，任何缺项给出的都是补齐命令，永不给"建议弃场" |
| R8 | 09-14 | 全段（审计） | **完整性审计（用户问「前面说到的有没有遗漏或被打断」）**：把架构设计文件、缺口体检文件、会话承诺逐条对账，**补登 5 项漏登**（5.11 概率校准器／5.12 分歧审计器＋台账／5.13 场景化分工／5.14 汇报通道／5.15 工具库瘦身） | ①实跑 `de_channel_send` → **0/0 渠道注册**、feishu「渠道未注册」②`_tmp_football` 顶层 `.py` 实测 **2597 个** ③`260914_V12最优架构设计.md` P0/P1 序 vs 总表逐条比对：**①概率校准器被我私自从 P0 降为 P2、②分歧审计器从未登记** | **发现头号遗漏＝汇报通道从未接通**（＝用户最初抱怨的根因）；口径不一致（私降优先级）与漏登项已全部补进总表；审计动作本身固化为一轮 |

## 审计发现的「被改向/打断」项（已按新口径处理，非遗漏）
| # | 原计划 | 变更 | 依据 |
|---|---|---|---|
| 1 | `3.1` 弃场/降级机制（信息不足→不出票） | 🚫 **否决** → 改为 `3.5` 全覆盖门禁（只补不弃） | 用户 9/14：「批次内的比赛几乎全部都分析，不应该有该不该出票这个情况」 |
| 2 | `1.2` 赛前 ~1h 首发核对 | 🚫 **撤销**（结构性不可得） | 用户 9/14：凌晨场开球时体彩已关门，首发晚于停售，对出票零作用 |
| 3 | `1.3` 信息量评分（支撑弃场） | 🟡 **口径改**：只检查+补齐，不阻断出票 | 随 #1 |
| 4 | V12 模型层接管判定 | 🚫 **我自己撤回**（32 组网格：方向 0/32） | `v12_sweep.py`；复评触发＝evidence 覆盖≥70% 或 n≥1000 |

## 待验证/待复评
- 兑现路径的分档阈值（75′/85′/86′）是**机械约定**，非回测产物 → 累积 ≥40 场后回测是否与"实际运气程度"相关；若相关弱则只作标注不参与任何统计。
- 过程分 5 项的**权重**未定（当前等权）→ 等有 n≥40 再定；**当前只报不评分**。

# Python
__pycache__/
*.pyc
*.pyo

# 绯荤粺
.DS_Store
Thumbs.db

# 涓存椂
*.tmp

_backup/
.wrangler/
*.log
__pycache__/

# 2026-09-16：原始对话导出移出公开仓库（11.4 MB，仅供本地回溯）
# 理由：作品集场景下，大体积原始 AI 对话转储会削弱真实工程工作的呈现。
# 本地文件完整保留在 D:\Cola\足球分析学习\06_对话转录\
06_对话转录/

# 批处理中间产物与备份副本（2026-09-16 补全）
# 背景：仓库曾有 52 个被跟踪的备份文件、共 30.42 MB（占全仓 41.4%），
#   其中 _发布_public/js/ 下 18 个（约 24.7 MB）会被一并打包进网站部署。
#   实测所有代码对 .bak 的引用都是 Copy-Item / shutil.copy2（**只写不读**）→ 可安全移出仓库。
#   ⚠ 不要用 `git add -f` 强行加回：这些是本地安全副本，不是产物。
*.bak
*.bak_*
*._bak*
*.draft
*.draft.json

# 2026-09-16：坏的子模块引用（github 上显示为打不开的链接）
# 该路径以 gitlink(mode 160000) 形式存在但仓库无 .gitmodules，
# CI 的 actions/checkout 会报 "No url found for submodule path"。
# 已从索引移除；本地克隆保留。技能库实体内容见同目录 `技能库_skills安装版/`，
# 上游：https://github.com/mattpocock/skills
07_技能学习/技能库_mattpocock完整仓库/

# 2026-09-21: backup copies + scratch verify outputs (local safety copies, not artifacts)
*.bak*
/_bs*.txt

# R2-L 例外（面板 #1 平局档不让位）＋ 025 并列政策｜2026-09-19 立（**0920 批生效**）

> 触发：**0919-007 奥萨苏纳 1-1 巴列卡诺**——R2 去平把**全场最高概率档 `1-1`（10.40%·面板 #1）**让掉、换入 `3-1`（6.06%），我又把 dir 同步收缩为单边，**方向与比分同时丢** ⇒ 四维由本可 3/4 掉到 **1/4**。
> 判据全部机器回源（回测脚本 `sat0919/r2l_test.py`，禁手填）。

## §1 R2-L 条款

**判据**：**被让出的平局档＝该场面板 #1（最高概率「数字比分」）时，R2 不生效**（保留平代表）；R6 大分档义务仍生效。
- **「#1」＝严格唯一**：平局档概率须**严格大于**最优非平档才算 #1；**并列不算**（避免用任意 tie-break 阻断发布）。
- **依据（回测）**：触发域＝含平档 ∧ λ比≥1.40，**n=101**
  | 策略 | 命中 | 净 |
  |---|---|---|
  | A 现行票面 | 20.8%（21） | — |
  | B R2 全去平 | **25.7%（26）** | **+5** |
  | **C R2-L（#1 保留）** | 24.8%（25） | **+4**（相对 B：B得C失 2／C得B失 1） |
  ⇒ 代价仅 **−1**（n=101 内不显著），换来与既有「**禁让掉概率更高的项**」自洽（让 #1 档本身即违规），且直接救回 007 型。
- **生效日闸门**：`COMP_COMPOSE_R2L_EFFECTIVE`（默认 2026-09-20）；检查器 `check_comp_compose.py` 自检 **22/22**、`--all` rc=0；装配器同源防线（`draw_no1()/r2l_warn()`，只写 warns 不改票面）。
- **近期实证**：007 的 `1-1`（10.40% 唯一 #1）⇒ R2-L 保留 ⇒ 票面 `1-1/2-1/3-1`（和 26.18%）＝本场 3/4；019/020 的 1-1 是 #2、029 是 #2、011/012 是 #3 ⇒ 不受影响。

## §2 025 并列 tie 政策（维持「严格唯一 #1」）

025 面板 `1-2` 与 `1-1` **并列 9.81%**。裁决：**并列 ⇒ 不算 #1 ⇒ R2-L 不触发**（维持现政策）。
理由：①不触发 ⇒ 不误阻断发布（已避免 arbitrary tie-break）；②并列意味着「平与非平无概率优势」，此时强制保留平代表＝用一条无信息优势的规则改票面，违「禁让掉概率更高的项」的**前提**（无更高者）；③回测侧无证据支持并列格特殊处理。
落地：夹具 `r2l_tie`（λ 取 025 实数 1.18/2.00 ⇒ **0E**）；三条理由已写入 `check_comp_compose.py` 的 `R2L_EFFECTIVE` 注释。

## §3 连带（同日已执行）

- **dir 收缩回退**：方向层**不因票面去平而收缩**（恢复 `主胜/平`、`客胜/平`；去平由让位声明承载，三处门禁 R2 豁免在位）。已重发**未开赛**的 019/020/025/029（v202609192203）；011/012（已开赛）、007（已完赛）不回改。
- **源侧回写**：specs/decisions 已按磁盘 data.js 回写；dry-run 全批 **票面/dir/dc diff＝0**（断层闭合）。

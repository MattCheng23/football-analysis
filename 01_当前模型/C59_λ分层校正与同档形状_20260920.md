# C59（候选·预登记）｜λ 分层校正 ＋ 同档形状修正｜2026-09-20

> 来源：用户转述的豆包分析框架，按项目纪律**只当假设、用全量台账机械检验**（`a29d4e7b` 结论必须回一手源）。
> 检验脚本：`sun0920\doubao_study.py`、`doubao_study2.py`｜唯一实现：`sun0920\lam_calib.py`｜预登记：`sun0920\c59_prereg.py`
> **状态：候选（未生效）**——本批已发布 ⇒ **票面不回改**，按 `59e64481` 转赛前预登记，批末用赛果检验，通过才转正式条款并同步检查器。

## 一、条款内容（拟）

### C59-a｜λ 低估按「λ比」分层校正（**仅 λ比 ≥ 1.40 生效**）
| λ比层 | n | λ和均值 | 实际总球 | **偏差 d_tot** | 该层 P(总≥4) 实际 vs 泊松 | **d_p4** |
|---|---|---|---|---|---|---|
| 同档 1.00–1.20 | 98 | 2.62 | 2.97 | +0.35 | 33.7% vs 26.9% | **0（不校正）** |
| 同档 1.20–1.40 | 100 | 2.59 | 2.60 | **+0.01** | 24.0% vs 26.7% | **0（不校正）** |
| 中差 1.40–1.80 | 84 | 2.74 | 3.07 | +0.33 | 46.4% vs 29.7% | **+16.7pp** |
| **大差 1.80–2.60** | 81 | 2.72 | 3.47 | **+0.75** | **43.2% vs 29.4%** | **+13.8pp** |
| 碾压 ≥2.60 | 33 | 3.26 | 3.91 | +0.65 | 54.5% vs 40.9% | **+13.6pp** |

- **显著性的关键对比**：大差层 +0.75（sd 2.07, n=81）vs 同档 1.20–1.40 +0.01（sd 1.62, n=100）
  ⇒ 差值 **+0.74 球，SE 0.281，t=2.63，双侧 p≈0.0085**。
- 全量 λ 偏差均值 **+0.37 球**（与 V11.53-10 记录的「λ 系统性低估 0.41 球」同源互证）。
- **判据**：`R6`（P(总≥4)≥30% ∧ λ比≥1.3 ⇒ 票面须含大分档）在 λ比≥1.40 时，先把该层 `d_p4` 加到模型 P(总≥4) 上再判。

### C59-b｜同档形状修正（λ比 < 1.40）
同档合并（n=198）：**零封率 实际 41.4% vs 泊松 48.2%（−6.8pp）**、**双方进球 58.6% vs 51.8%（+6.8pp）**，
且**两个子带同向**（1.00–1.20：−9.8pp；1.20–1.40：−3.8pp）⇒ 成立。
**判据**：λ比<1.40 ⇒ **零封档降权（−6.8pp）、双向档优先（+6.8pp）**，与既有 R6／C57-v2 方向一致，但给出分层量化幅度。

## 二、自查抓出并已修正的一处内部不一致（记录在案）

首版把「同档」也纳入 C59-a 校正（取 +6.8pp）。**自查发现两个同档子带的 P4 偏差方向相反**
（1.00–1.20 **+6.8pp** vs 1.20–1.40 **−2.7pp**）⇒ 合并均值 ≈+1.5pp、**无判别力**。
按纪律（小样本/不显著 ⇒ 不改规则）**改为同档不校正**，形状修正则因两子带同向而保留。
⇒ 教训：**分层条款必须逐子带查同向性，合并均值会掩盖符号相反的层**。

## 三、0920 批预登记结果（`C59_预登记_20260920.json`）

- **C59 会改变 R6 判定的：8 场**——003 / 005 / 008 / 019 / 022 / 023 / 025 / 026
  （均为「按现行 P(总≥4) 判不触发、按 C59 校正后 ≥30% 触发」；其中 005 与 025 的 p4 现为 29.7%／28.6%，距 30% 线仅 0.3／1.4pp）。
- **票面不回改**（本批已发布＋部分已开赛，`0b38b571` 开赛后锁定）。
- **批末检验口径**（`python -X utf8 -u c59_prereg.py --verify`）：
  对上述 8 场统计「实际总球 ≥4 的场数」；对照同档层（不校正）的实测比例。
  登记时该 8 场的模型期望 P4 均值约 28%；若实测明显更高 ⇒ C59-a 成立并转正式条款；否则否证并入档。

## 四、配套落地的两项（吸收框架里我们**真正缺**的部分）

| 项 | 文件 | 说明 |
|---|---|---|
| **风格标签** | `mk_style_tags.py` → `style_tags.json` | 从 digest 官方积分表逐批生成：**ATK**（场均进≥1.70）／**DEF**（场均失≤1.00 且进<1.70）／**OPEN**（进与失同时≥1.30＝对攻型）／**MID**／**NA**。0920 批 60 队：ATK 15／OPEN 13／MID 22／DEF 6／NA 4。**标签＝当前赛季形态描述，禁外推**；用途＝「同风格对手样本提取」与形状声明 |
| **德比对照表** | `derby_table.py` → `derby_table.json` | 人工维护（32 条：同城 17／地区 11／国家 4），**匹配＝队名子串对**。0920 批命中 **4 场**：007 黑乡德比／016 马德里德比／027 法国国家德比／029 葡萄牙国家德比。⚠ **性质声明**：它只说「这是德比」，**不说**该怎么判；与 `评级过严审计_20260907`（德比属轻微因素、**不应降 A 级**）的冲突**待检验**，故 **禁止**仅凭德比降评级 |

## 五、接线（已改代码·**未重跑 0920 批**）

`mkspec.py` 已新增三行机算正文（每场）：
1. **【C59 校正】** λ比 ⇒ 层（n）／校正幅度／形状提示
2. **【风格标签】** 主 vs 客（标签＋场均进失＋排名＋轮次）⇒ 是否同风格对垒
3. **【德比】** 命中时出「德比登记」句（且显式声明**不据此降评级**）

⚠ **0920 批的 specs 不再重跑**（已发布＋部分开赛）⇒ 这三行从**下一批**起出现在正文。
三项都带 `try/except` 且失败时**显式写出「未机算」**（禁静默通过）。

## 六、复算命令

```
python -X utf8 -u D:\Cola\_tmp_football\sun0920\doubao_study.py     # 分层偏差 + 主张 A/B 检验
python -X utf8 -u D:\Cola\_tmp_football\sun0920\doubao_study2.py    # 按 λ比/λ和 的总球偏差
python -X utf8 -u D:\Cola\_tmp_football\sun0920\lam_calib.py 0.80 1.95   # 单场 C59 建议
python -X utf8 -u D:\Cola\_tmp_football\sun0920\c59_prereg.py       # 重算预登记
python -X utf8 -u D:\Cola\_tmp_football\sun0920\c59_prereg.py --verify   # 批末检验
python -X utf8 -u D:\Cola\_tmp_football\sun0920\mk_style_tags.py    # 重建风格表
python -X utf8 -u D:\Cola\_tmp_football\sun0920\derby_table.py      # 重建德比表 + 查本批命中
```

# PTC 开场指令（交接模板·2026-09-16）

> 用途：把「标准模式会话里的我」无损交接给**新的 PTC 会话**。操作只有三步，核心是**粘贴一段话**。
> 配套：`01_当前模型/PTC模式运行纪律与继承清单_20260916.md`（完整清单）｜项目 key 记忆已含本路线（自动注入，无需你复述）。

---

## 一、你要做的三步（网络稳的时候做）

1. 预设面板点 **PTC 模式**（它暂存为"下一个会话"的组合；当前会话不受影响）。
2. **新建会话**，工作目录填 `D:\Cola`。
3. 把下面 §二 的**开场指令**整段粘进去发出去。就这一句，其余它自己按记忆＋文件接上。

> ⏱ 时机：随时可切。**盯场器是独立进程**（见 §三），换会话不影响它收赛果；唯一禁忌＝**别让新会话再起第二个盯场器**（一批只能一个写入者）。

---

## 二、开场指令（复制这一段即可·已自包含）

```
接手 0915 批（竞彩足球）后续工作。先按顺序读这四份文件再动手：
D:\Cola\_tmp_football\tue0915\待模型收尾.md、
D:\Cola\_tmp_football\tue0915\自动入库_待补归因.json（非空即先补收尾）、
D:\Cola\_tmp_football\tue0915\批级待办_20260915.md、
D:\Cola\足球分析学习\03_报告复盘\20260915_进度单.md；
然后读 D:\Cola\足球分析学习\01_当前模型\PTC模式运行纪律与继承清单_20260916.md 并全程遵守其中 4 条纪律
（D1 大文件/长行禁走 read，一律 pwsh→python 中转只读结果；D2 读 .ps1 等 UTF-16LE 先验头两字节并转码；
D3 中文输出落文件再读；D4 长任务甩 run_in_background，run_code 单次墙钟 600s）。
注意：0915 批的盯场器已由上一会话启动并在跑（python auto_watch_batch.py，PID 见批级待办），
**不要再起第二个盯场器或第二个写入者**；每场终场它会自动入库＋部署，你负责模型侧收尾。
任务是：① 等 014 终场后跑 python -u _tmp_football/batch_end.py 2026-09-15（含第二源回补）；
② 联赛层蒸馏（西甲 007/008/013 达 3 场、英联赛杯 011/012 记实证行）；
③ 整体层总账（四维总账＋λ 偏差＋归属 M/L/G＋本轮候选批级命中）；
④ 闸门复核 preflight_gates.py 2026-09-15 应 7 项全绿、台账闭环（第二源待补 无、未闭环 0）。
每轮收尾必须做三件套：memory（daily+project 一次调用）、de_channel_send channels=feishu（对话镜像）、present（交付物）。
汇报给 Cola_9.7：真实命中率只报实测值，表格存文件＋对话精简版（≤5 行×5 列）。
```

**极简版（若上面太长）**：
```
接手 0915 批：先读 D:\Cola\足球分析学习\01_当前模型\PTC模式运行纪律与继承清单_20260916.md 与
D:\Cola\_tmp_football\tue0915\批级待办_20260915.md，按其中纪律与待办执行；
盯场器已在跑（勿再起第二个）；收尾三件套（memory＋飞书镜像＋present）照做。
```

---

## 三、本次交接状态（会变·切换时按最新版说明）

| 项 | 值 |
|---|---|
| 批键 | `2026-09-15`（周二批，14 场） |
| 已入库 | **7/14**（001–007） |
| 未入库 | 008–014（010–013 进行中；014 约 06:00 开赛 / 07:50 终场） |
| 盯场器 | `python -u auto_watch_batch.py --batch 2026-09-15 --dir tue0915 --ids fm_ids_flat.json --every 120 --max-min 660`，**PID 27468**，覆盖至约 08:18 |
| 第二源待补 | 004–007（官方未开奖；`sun0913/ss_tool.py apply 2026-09-15 --apply` 回补） |
| 无异常 | `部署失败_报警.json` 不存在 ✅ |
| 剩余模型侧 | 批末总账／联赛层（西甲、英联赛杯）／台账闭环／C38 口径已修（§三·补8） |

---

## 四、切过去之后的第一次验收（判"继承无遗漏"）

用**同一个批键**在 PTC 会话跑：`batch_end.py 2026-09-15` → `preflight_gates.py 2026-09-15`（7 项）→ 一场复盘入库 →
把输出与标准模式同一批的输出**逐行 diff**：**差异 0** 才算继承完整；若出现差异，先判"样本差（快照） vs 实现差"并写明。
另：**B13 浏览器通道**冒烟（清单文档 §六）未过之前，发布链的"页面渲染验证"一步留在标准模式或改用 HTTP 回查。

# 流程体检优化清单（2026-09-05 四路并行审计 · mattpocock 评审双轴法）

> 四路：流程 / 工具链 / 数据管道 / 自身习惯；共 38 条发现。状态：✅已修 / ⏳待修（含原因）。
> 审计方法=参照 mattpocock/skills"双轴评审+证据驱动"：每条带证据路径，修复≤3 行优先。

## A. 高优先已修（本次完成）

| # | 发现 | 修复 | 状态 |
|---|---|---|---|
| A1 | check_batch_full 预警区 CRLF 整段静默跳过 | 读文件统一 `\r\n→\n` | ✅ |
| A2 | 0e2 bigSeven 条目无 logic 字段→0 匹配静默 | 正则去掉 logic 要求 | ✅ |
| A3 | V11.29-5 alerts-ht 字段序不匹配→从未生效 | 兼容 no 前/后两种序 | ✅ |
| A4 | 预警覆盖正则依赖数组顺序+缺失无提示 | `_grab_arr` 按标识独立定位+缺失计问题+全覆盖校验 | ✅ |
| A5 | 检查器恒 exit 0（发布靠人肉读 txt） | 机器可判定退出码（OK=0/FAIL=2）+RESULT 行 | ✅ |
| A6 | deploy_publish.ps1 版本正则 `\?v20\d{12}` 永不匹配+验证假阳性 | 正则 `\?v\d{12}`+替换计数断言+验证首页含 `?v=$v` | ✅ |
| A7 | deploy_publish.ps1 PS5.1 中文乱码（无 BOM） | 转 UTF-8 with BOM；写 HTML 用无 BOM UTF8 | ✅ |
| A8 | bump_version.py 漏 bill.html | 补入 | ✅ |
| A9 | fetch_wb_sat 校验 `key in val` 无归一化（007/023 重捕） | norm()（NFKD+ø/æ/ß 音译）+FAIL_ALL 计数修正 | ✅ |
| A10 | satmap 归一化对 ø 失效+只用短名（012/013/019 失配） | norm 音译+longName 双候选 → **29/29 全命中** | ✅ |
| A11 | satverify 只查前缀不查值非空+H2H 截断无标记 | v2：值非空校验+全史>40/全史=0 检出→gaps.txt | ✅ |
| A12 | satrep 汇总 9 列表头只写 5 列数据 | v2：9 列全出+附加缺口清单 | ✅ |
| A13 | app.js 复盘回填读顶级键（新批 predict 内→静默失效） | 兼容 predict/顶级两结构+`||[]` 容错 | ✅ |
| A14 | 技能版本漂移（batch-pipeline V11.30/pre-match V11.33/速查表 V11.29 权威） | 三处全部对齐 V11.34+加"以最新为准" | ✅ |
| A15 | 复盘门2 目标文件冲突（data.js vs data-review.js） | post-match 门2/步骤8 改写 data-review.js REVIEW_EXTRA | ✅ |
| A16 | 门3 未指向 verify_online.py | 补入+工具清单 | ✅ |
| A17 | 备料未成标准动作+脚本未登记 | batch-pipeline 新增备料标准动作（验收=场数+无 FAIL_ALL+缺口清单）；速查表登记批级备料三件套 | ✅ |
| A18 | failure_log 自 8/25 零更新 | 回填 12-16 五条（9/2-9/5） | ✅ |
| A19 | 速查表派活仍按 8/25 旧角色（违协议 v3） | 加协议 v3 优先注记 | ✅ |

## B. 待修（建议下轮，按优先级）

| # | 发现 | 建议 | 优先级 |
|---|---|---|---|
| B1 | 备料 README"已就绪"覆盖缺口（013 全史=0/002 三字段空） | satrep 缺口清单并入 README（本次已生成 gaps.txt，尚未回写 README） | 高 |
| B2 | H2H 行丢 league 字段（杯赛无法自动过滤）+未来未赛行计入 | 捕捉 CODE 加 `(x.league||{}).name`+finished 过滤，下批生效 | 高 |
| B3 | 无断言链：satlist/satmap 失败也 exit 0 | 各脚本末尾断言场数/匹配数，失败 exit 1 | 高 |
| B4 | tpl_insert 模板（备份+count 断言+ast）退化 | 固化 tpl_insert.py，新批只改数据段 | 高 |
| B5 | 近况数据来源审计（正式赛过滤）未内建 | 装载脚本输出来源+剔除清单；检查卡加近况审计 | 高 |
| B6 | 内联 python -c 大规模重犯 | 纳入黑名单自查（每日收尾一问）+补丁工具柜 | 中 |
| B7 | 验证链碎片化 | 固化 verify_batch.py 四连（node+check+覆盖行+线上） | 中 |
| B8 | fetch_wb_tue_v2 未收敛（NameError/关组相反） | 统一参数化模板（本轮已并入 fetch_wb_sat，tue 弃用） | 中 |
| B9 | 9/3 补丁链 9 连改-改错门 | 初稿快照+diff；补丁>2 轮强制回生成器 | 中 |
| B10 | poisson_top/ht_top 固定输出互相覆盖+交叉校验仅文本 | --out 参数+--score 自动断言 | 低 |
| B11 | 官方排期可直连 curl（少一跳 WebBridge） | 下批改直连+satlist docstring 修正 | 低 |
| B12 | 协议 v3 全文回灌岗位卡 | 岗位卡 v3 更新（已加注） | 低 |

## C. 已确认无害/设计如此
- 9/3 批 003/004 0e2 报警（已完赛历史批未联动 ou/比分）：检查器现在**正确**抓出；历史批不回改，复盘验证用
- H2H<3 场次（005/007/013）：升班马/稀有对碰，R419 口径
- 002 无天气/球场：FotMob 该场未公布，分析时人工补（open-meteo 城市=山形/甲府）

## 依据
- workflow "pipeline-audit"（4 agent，2026-09-05）；全量发现文本见会话产物
- mattpocock/skills 方法论：writing-for-agents（完成标准可检验/正向表述）、code-review 双轴（标准轴+规格轴并行）

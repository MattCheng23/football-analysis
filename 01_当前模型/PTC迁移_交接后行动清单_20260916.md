# PTC 迁移·交接后行动清单（2026-09-16 04:05 生成）

状态（04:11 更新）：PTC 第一段完成，第二段进行中——`results=9 = evidence=9 = ⑪标记=9`（001–009），DONE=9／PEND=0，`node --check` exit 0；
缺 010 / 011 / 012 / 013 / 014（010–013 终场窗口 04:35–05:20，014 约 07:50）。**B13 已于 04:05 在 PTC 内通过（迁移缺口清零）**。盯场器 27468 + 守候 pwsh-18 在跑。

---

## 一、当前在跑的两个自动化（铁律：禁再起第三个写入者）

| 任务 | 起于 | PID | 作用 | 覆盖到 |
|---|---|---|---|---|
| 盯场器 `auto_watch_batch.py`（标准模式＝本会话） | 09-15 21:18 | 27468 | 每 120s 拉赛果 → 入库 → ⑪ 归因 | 约 09-16 08:18 |
| 守候 `_ptc_waiter.py`（PTC 会话 pwsh-18） | 09-16 03:55 | 1732 | 每 4 分钟读 `data-review.js`，见到 014 即自动跑批末链 | 无时限（014 入库为止） |

**双写者禁令**：PTC 会话不得另起盯场器；本会话不得再起第二个 waiter。两边各只有一个写入者。

---

## 二、接下来会自动发生的事（用户无需操作）

| 时间 | 事件 |
|---|---|
| 04:45–05:20 | 010（02:45）/ 011、012（03:00）/ 013（03:30）终场 → 盯场器入库（`results` 8 → 12） |
| ≈07:50 | 014（06:00 开赛·本批最后一场）终场 → 入库瞬间触发 PTC 的 pwsh-18 |
| 014 入库后自动链 | `batch_end.py 2026-09-15`（含第二源回补）→ `preflight_gates.py`（7 项）→ `verify_second_source.py`，输出 `_tmp_football\tue0915\ptc_wait_out.txt` |

---

## 三、只需要的 3 个动作

1. **A/B 对照验收（我方·标准模式）**——PTC 报"批末链完成"后，在本会话对同一批重跑 `batch_end` + 7 项门禁，与 PTC 输出逐行比对，要求 **0 行差异**。这是"完美继承"的硬验收。
2. ~~**B13 浏览器通道冒烟（PTC 会话）**~~ → **✅ 已通过（2026-09-16 04:05）**：daemon `127.0.0.1:10086`，`navigate`(tabId 307709431) + `evaluate` 取回 title／h1／cards=6／版本 `v202609160359`；我方独立复核本地 `_发布_public` 版本串同为 `v202609160359` → 读到的是当前线上页。**迁移缺口清零，发布链页面验证改为默认在 PTC 内执行（HTTP 回查兜底）。**
3. **异常回传**——若 PTC 汇报出现：门禁非 7 项全绿 / 第二源待补非「无」/ 台账未闭环非 0 / 008 到 05:00 仍未入库 → 把该段结果贴回本会话，我来查（只查不写，守一写者）。

---

## 四、PTC 自办的批末清单（我方只核对，不代做）

1. 联赛层蒸馏：西甲 007/008/013 三场 + 英联赛杯 011/012 实证行
2. 整体层总账：四维（方向/比分/ht/ou）+ λ 偏差 + 归属 M/L/G + 候选批级命中
3. 闭环核验：7 项门禁全绿、第二源待补=无、台账未闭环=0
4. 收尾三件套：memory（daily+project）→ 飞书直发 → present

---

## 五·补、可粘贴指令（完整版·直接发给 PTC 会话）

【0915 批·第二段交接指令（来自标准模式会话 Cola_9.7／session-4b7cd565，09-16 04:10）】你现在是 0915 批的接手方，第一段你已完成（004-007、009 的 ⑪ 深度归因已入 `data-review.js` evidence 并核销，标记=8、待办空），当前 `results=8`、缺 008/010/011/012/013/014；请按以下顺序继续、不要跳步：①**禁再起任何第二个写入者**——标准模式会话的盯场器 PID 27468（每 120s，覆盖到约 08:18）与你的守候 PID 1732（pwsh-18）各一个，一写者铁律；②**008 观察线**（02:00 开赛、比同类慢约 13 分钟）：仅当 05:00 后仍未入库，才去读盯场器单场日志定位，且只查不写；③**010/011/012/013**（02:45 / 03:00 / 03:00 / 03:30 开赛，约 04:45–05:20 终场）每场一入库就按纪律立刻做深度归因＋evidence 核销（不等批末），写完核对「标记数 == results 数」；④**014**（06:00 开赛、约 07:50 终场、本批最后一场）入库即由你的 pwsh-18 自动串起 `batch_end.py 2026-09-15`（含第二源回补）→ `preflight_gates.py`（7 项）→ `verify_second_source.py`，输出写进 `_tmp_football\tue0915\ptc_wait_out.txt`；⑤跑完批末链后补齐剩余：联赛层蒸馏（西甲 007/008/013 三场＋英联赛杯 011/012 实证行）→ 整体层总账（四维＋λ 偏差＋归属 M/L/G＋候选批级命中）→ 闭环核验（7 项全绿、第二源待补=无、台账未闭环=0）；⑥**做一次 B13 浏览器通道冒烟**（迁移清单里唯一未验证缺口）：用浏览器通道对线上页面各做一次 navigate 与 evaluate，通过就记「B13 通过」，不通过就记「B13 不通过＋失败现象」，渲染回查先用 HTTP 回查替代；⑦**冻结批末脚本**——标准模式会话稍后会对同一批重跑 batch_end＋7 门禁做 A/B 逐行比对（要求 0 行差异），因此在这轮 A/B 结束前请不要改动 `_tmp_football` 下的 `batch_end.py`／`preflight_gates.py`／`check_*.py`／`batchseg.py`，若发现必须改，先停下并在汇报里写「建议改动＋理由」，等 A/B 基线跑完再动；⑧按三件套收尾（memory daily+project 一次调用 → `de_channel_send channels=feishu` 直发 → present）；⑨**最后给我一段可粘贴的回报**，必须含：批末链是否跑完＋输出文件路径、7 门禁逐项结果、第二源待补与台账未闭环数字、各场核销后的标记数、B13 结果、以及任何未完成或存疑项。入口文件：`01_当前模型\PTC迁移_交接后行动清单_20260916.md`、`01_当前模型\PTC模式运行纪律与继承清单_20260916.md`、`03_报告复盘\20260915_进度单.md`。老规矩不变：只报真实命中率、开赛后票面锁定、赔率/盘口不作预测依据（R343）。

（极简版：只保留 ①④⑥⑦⑨。）

---

## 五、继承边界（一句话确认）

memory 5 轨、dtodo、飞书渠道、技能库、项目文件、除 `workflow` 外的全部工具、人格与 runtime-context 注入——都随 PTC 预设保留；
**唯一不继承的是本会话的对话上下文**，所以状态一律写在文件（进度单／台账／交接文档）里，续跑靠读文件，不靠模型记忆。

---

## 六、A/B 协议（提前版·2026-09-16 04:55 用户要求「不用等 014」后定）

**先说判据的修正**：原写"输出 0 行差异"**过强**。读了 `batch_end.py`（99 行）后确认：

- 它是**可重跑**的：② 台账入账幂等（跳过已存在）、⑤ 进度单与 ⑥ 总账草稿每次**重写**；
- 因此**先跑的一方**会打印"新增 N 条"，**后跑的一方**必然打印"跳过" → **ingest 步的 stdout 天然不同，这不是缺陷**。

### 6.1 判据＝比产物（三条）

| # | 比什么 | 怎么比 |
|---|---|---|
| 1 | `03_报告复盘\20260915_批末总账_草稿.md` | 剔除第 3 行「生成：…」后逐行 diff（内部含各步 rc／尾 40 行／四维数字） |
| 2 | `03_报告复盘\20260915_进度单.md` | 剔除「生成时间」行后逐行 diff |
| 3 | `preflight_gates.py 2026-09-15` 7 项 | 逐项结果 diff |

### 6.2 硬条件＝输入指纹一致

两次运行期间 `data.js`／`data-review.js`／`v12db_full.json` 的 **SHA256＋mtime** 必须逐个相同；窗口内**不得有 013/014 落地**。
命令（两侧都跑）：`Get-FileHash <三个文件> -Algorithm SHA256` ＋ `Get-Date`。
指纹不一致 → 后跑一方**判无效并重跑**（单次链约 1–2 分钟）；仍撞车则退到**备用路径**：与 PTC 在 014 落地后的**正式批末**对跑。

### 6.3 顺序（一写者，禁并行）

PTC 先跑（批主）→ 回报（指纹＋7 门禁＋产物路径与关键数字＋墙钟）→ **标准模式会话在同一窗口内后跑** → 比产物 → 指纹一致且产物 0 差异 ＝ **继承验收通过**。

### 6.4 提前跑不污染批末（已核对脚本行为）

`batch_end.py` 只做：①第二源回补（只回填官方已开奖且与我方一致的场次）②台账幂等入账 ③四维结算 ④复盘门禁 ⑤重写进度单 ⑥重写总账草稿。
**不部署、不发通知、不封批**；014 落地后再跑一次正式批末会**覆盖**成 14/14，属正常路径。

### 6.5 顺带量化（§6.5 of 跨会话协作文档）

两侧会话的 `sessionStats`／`tokenUsage`（`~/.dsh/storages/session_projcache/sessions/session-<uuid>.json`）＋墙钟 → 产出「同批链：PTC x 调用/y 未缓存输入/z 输出/w 墙钟；标准 A/B/C/D」。

### 6.6 可粘贴指令（发给 PTC·半批 A/B）

【0915 批·半批 A/B 指令】012 入库后**不要等 014**，立刻做四件事：①记录输入指纹：`Get-FileHash D:\Cola\足球分析学习\_发布_public\js\data.js, D:\Cola\足球分析学习\_发布_public\js\data-review.js, D:\Cola\足球分析学习\05_模型代码\v12db_full.json -Algorithm SHA256` ＋ `Get-Date`；②跑 `python -u batch_end.py 2026-09-15`，再跑 `python -u preflight_gates.py 2026-09-15`（**脚本冻结、不改**）；③回报四项：输入指纹、7 门禁逐项、总账草稿与进度单的路径＋关键数字（入库 N/14、第二源待补、深归因缺项、四维）、本次墙钟；④跑完**继续守候 014**，014 落地后再跑一次正式批末（会覆盖草稿，正常）。判据＝**产物 0 差异**（不比 ingest 的 stdout：台账入账幂等，先跑方"新增"、后跑方"跳过"，天然不同）；标准模式会话会在同一窗口内后跑同一对并逐行比产物。

### 6.7 首轮实跑结果（2026-09-16 04:57·按协议判「无效」，但机制等价已有强证据）

| 项 | 先跑方（PTC） | 后跑方（标准·本会话） |
|---|---|---|
| 运行时刻 | 04:55:23 | 04:57:41 |
| 输入状态 | results=11 | **results=12** |
| data.js 指纹 | （PTC 报 e5d8fdf3…系 data-review） | `205b604a98671cef` mtime 04:57:28 |
| data-review.js 指纹 | `e5d8fdf3…` → 后补 ⑪ 改 `5a033a6e…` | `a19ce07e9d3ed41f` mtime 04:57:28 |
| 7 门禁 | 全绿（自报） | **全绿（我方独立复现：ERROR 0；WARN 3/10/1/1/0/0）** |
| 台账 | 631 条（0915 入账 11 场） | 632 条（0915 入账 12 场） |

**判无效的原因**：011（利物浦 vs 热刺）于 **04:57:28 落地**，比标准侧开跑早 13 秒 → 两侧输入指纹不一致，按 §6.2 硬条件**判无效重跑**（不是失败）。

**46 行差异的逐条分类——100% 由 011 落地解释，无一行属"机制差异"**：
入库 11→12 ｜ reviewedCount 11→12 ｜ 双日志行 21/32→22/33 ｜ 结算表新增 `011 利物浦 3-1 热刺 lg-ef 1/4` ｜ 命中率 n=11→12（方向 72.7→75.0%、四维 20/44→21/48）｜ 联赛分组 lg-eflc 1→2 场 ｜ 未入库 011,013,014 → 013,014 ｜ 深归因缺项 012→011 ｜ 第二源待补清单 +011 ｜ 台账 626→631→632、新增 5→1 条。

**标准侧运行的写入面**（与 PTC 侧同形）：台账 +1 条（→632）；`data.js`／`data-review.js` **未被我的运行改动**（第二源回补回填 0 场）。

**结论**：机制等价**有强证据**（全部差异可归因、无格式/逻辑差异、7 门禁双向一致），但**定论要走 014 后的正式批末对跑**——那是本批唯一无竞态的窗口（013 ≈05:25–05:35 落地会再次制造竞态，中途不必来回）。
首轮完整报告（含两侧指纹/stdout/逐行 diff）：`_tmp_football\tue0915\ab\ab_report_045741.md`。

**顺带得到的批况（n=12）**：方向 9/12=75.0% ｜ 比分 3/12=25.0% ｜ ht 4/12=33.3% ｜ ou 5/12=41.7% ｜ 四维 21/48=43.8% ｜ 满分场 1 ｜ 0/4 场＝008、012；011 利物浦 3-1 热刺＝1/4、012 伊普斯维奇 2-4 阿森纳＝0/4；深归因缺项＝011（PTC 观察线接管）。

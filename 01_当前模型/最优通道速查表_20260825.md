# 最优通道速查表（开工第一查！每个任务先查此表，已有通道路径直接复用）
> 原则：**先查表，再动手**——表里有的通道禁止重新探测/重新写脚本（除非验证失效）。
> 更新铁律：每打通新通道→当场登记（任务/路径/脚本/要点/日期）；每发现绕圈→回填"绕圈记录"并核查为何没查到。

## 工具选择节（同类工具先选对的，禁绕圈）
| 任务 | 首选 | 次选 | 要点 |
|---|---|---|---|
| 网页/文章正文 | read_page | WebBridge | 静态页直读；JS 渲染读不到→WebBridge |
| 网上搜最新消息/伤停新闻 | Google 搜索（WebBridge+Ghelper） | web_search | web_search 限流时换 Google；摘要不可靠→读原文 |
| 浏览器实读/渲染验证/官方站（EdgeOne WAF） | WebBridge navigate→evaluate/screenshot | curl+Googlebot UA | 第一优先（8/24 拍板）；curl 仅批量补位 |
| 批量结构化 JSON | FotMob API（matches?date / matchDetails） | 官方 webapi（仅 WebBridge 内 fetch） | x-mas header；matchDetails 自带 /match/{id} 直达 |
| 多场情报并行核查 | 常驻工作组（scout/loader/analyst/qc/archivist） | subagent_pro 临时 | 成员只提供信息不判断；判断权=主代理+用户审批 |
| 大量同构子任务（≥6 项审计/校对） | workflow 脚本编排 | subagent 逐个 | 有并发/总数 caps；脚本只协调 |
| 图片识图 | read_image（当前模型=V4-Flash-Vision-Exp 直接看图） | modlens_read_image（OCR/长表格转录） | 8/25 用户确认；modlens 仅结构化转录备用 |

## 数据装载类（新批次）
| 任务 | 最优通道 | 脚本/入口 | 要点 | 验证 |
|---|---|---|---|---|
| 拿 matchId | `matches?date=YYYYMMDD`（队名英文匹配） | curl + 1 次返回全天 | Bodø 特殊字符；中文名 0 命中 | 8/25 |
| 单场全量（赛前/赛后） | matchDetails?matchId= | fetch_md_tue.py（模板） | weather/h2h/teamForm(二维展平)/lineup/unavailable 全取 | 8/25 |
| **SSR 全量（H2H 全史/草皮/裁判/sc 赛果）** | WebBridge navigate `/match/{id}`→7s→evaluate | fetch_wb_tue_v2.py（**含队名校验+3 次重试**，禁 v1 无校验版） | 赛后事件流/评分也走此页 | 8/25 |
| 积分榜 | matchDetails content.table.leagueId → `leagues?id=X&tab=standings`（**顶层 table[0].data.table.all**，idx=排名） | fetch_standings.py | **韩职必须 parentLeagueId=9080**（919356 无效）；比赛（英联杯/欧冠资格赛）无积分榜属正常 | 8/25 |
| 积分榜兜底（leagueId 没拿到） | content.table.parentLeagueId | — | 与 leagueId 同源 | 8/25 |

## 分析工具类（已有全自动链）
zoned_predict（带判定）→ **band_gate（画像门·V11.12：大胜画像触发→研磨禁用）** → lambda_est → **lambda_scene（场景修正·V11.12：跨级/连胜/弱队外战/火力/H2H4球史=强方+0.3~0.5）** → poisson_top（比分+ou）→ ht_top（半全场）→ check_five_dim（泊松结构人工比对）→ check_batch_full（结构性 ERROR 拦截）——**每场全跑**（8/22 固化；V11.12 新增 band_gate/lambda_scene）；批后：audit_ht_0822（半全场全量审计）+ batch_distill（蒸馏三合一）。**🎯 终局目标（2026-08-27 用户拍板 V2.2 + 8/28 修：方向 80%·比分 50%（40 及格）·ht 75%·ou 65%）；优化模式（2026-08-28 用户拍板"全量回测已经够了"）=**针对性优化**：赛后复盘→错因统计→蒸馏 V11.x→**针对性验证该批错场（重点场+抽样防新伤组合，不再全量 158/161 场盲判）**；守擂清单 14 场仍每轮对账。下批开工先读 V11.29（2026-08-30 权威版：V11.27 四条+V11.28 三条+V11.29 八条——8/29 周六批 21 场复盘后定稿；A 级 5 场 1/5=方向崩盘主因；核心=**A 级对手反制四问/R431 三合一/克星=方向层信号/ou 上探双向对冲/alerts-ht 联动/R327 先核 xG**；模拟救回=方向 10→14/比分 5→17/ht 9→14/ou 6→12；方案层=德甲大分专攻·对攻场三条件入选法）** + 检查卡⑥B2 段**。
- ⚠️ zoned_predict 已知项（2026-08-25 修复+登记）：①并列带（<0.8）时建议区曾误打非判定带（已修：建议区严格按判定带+并列附备用行）②研磨带分支 print 曾因裸 % 崩溃（已修）③lambda_est rank-λ 基准对低产队（近5场均<0.8球）失真→**产量 λ（近5进失球直接算）优先，工具 rank λ 仅参考**（001 金泉实证：工具 λ和 2.30 vs 产量 1.7）

## 生成/发布类
| 任务 | 最优通道 | 脚本 | 要点 |
|---|---|---|---|
| 生成批段 | 改 gen_tue_batch.py（单源）→ python 执行→ node --check → check_batch_full | gen_tue_batch.py（8/25 批模板，可复制改） | 幂等（重复跑自动删旧段）；中文字符串禁半角引号 |
| 版本号 | python bump_version.py <真实时间 HHmm> | bump_version.py | 3 html 同步；时间=Get-Date 实测 |
| 部署 | wrangler pages deploy _发布_public | 发布报告中心.ps1（ps1 编码若坏直接手跑 wrangler） | 部署≠git 推送 |
| 线上验证 | 专属 URL（{hash}.pages.dev）+curl | verify_online.py（改 key 检查） | 必须 -sL；SPA fallback=200 需查 content |

## WebBridge 通用
- 请求=唯一名 JSON 文件 + `curl.exe --data-binary @file`（禁内联）
- **可用 tool 清单（别猜）**：navigate/find_tab/evaluate/network/snapshot/click/fill/mouse_click/cdp/key_type/send_keys/screenshot/save_as_pdf/upload/close_tab/list_tabs/close_session——**无 status**（查状态用 list_tabs）
- navigate 后 evaluate 前：验队名（不一致=重试）；组关闭时机：阶段完成即关（v3 约定）
- 外网：Ghelper 已装（Google/外文源可读）

## 常驻工作组分派（每批复用，禁新建临时子代理）
| 岗位 | id | 职责 | 产出 |
|---|---|---|---|
| scout 情报员 | 92c3437d | 多场并行联网核查（伤停逐人/天气时点/H2H 全史/战意，证据分级+⚠️冲突情报单列） | workgroup\scout_<批>_<日期>.md |
| loader 装载员 | 7e7a10a3 | FotMob/WebBridge 批量装载+自检（缺口清单） | workgroup\loader_<批>_<日期>.md |
| analyst 分析员 | 95c141ec | 独立五维推演（跑工具链，差异点给证据链） | workgroup\analyst_<场>_<日期>.md |
| qc 质检员 | d11d5e35 | 反向挑错 8 问（漏洞级） | workgroup\qc_<批>_<日期>.md |
| archivist 记录员 | d6cb840a | 归档+成员质量反馈回填岗位卡 | workgroup\archive_<批>_<日期>.md |

派活=先发两份卡（工作组方法论卡_20260825 + 工作组岗位卡_20260825）再按 **工作组派活模板_20260825** 填场次；流水线 scout+loader 并行 → analyst → qc → 组长裁决(用户审批) → archivist；判断权=组长+用户（成员只供信息/推演/挑错）。

## 复盘/蒸馏类
batch_review.py → ft_cards_check.py（红牌）→ redblack_analyze.py（红黑榜）→ league_calib.py → score_ou_deep.py/lambda_dev.py/ou_over.py（比分/ou 三件套）→ audit_ht_0822.py → 蒸馏 V11.x → 执行校准（新批先读）
- **命中率统计**：近 7 天=stats_near7.py（8/18 起口径，含批次明细）；全量重算=recompute_global.py（数据驱动，与 GLOBAL_STATS 对照）；遗留批（results 空）补录参考 gen_review_0820.py 模式

## 写代码纪律（防坑前置）
- 所有 .py：write 工具写 → `python -c "import ast;ast.parse(open(...,encoding='utf-8').read())"` **改为 write ast_check.py**（用脚本文件校验，禁内联）
- 批量 fix：幂等（new 已在=SKIP）+count==1+ast 校验+试跑
- 中文字符串：全角「」；可疑字符 chr() 构造

---
## 绕圈记录（有则回填：日期/绕了什么/为什么没查到=索引缺口）
- 8/25 上午：K1 积分榜 id 绕 3 圈（919356→36→9080）→ **缺口：速查表缺"韩职 9080"条目**（已补）
- 8/25 上午：积分榜解析路径探测 3 次 → **缺口：leagues?id 响应顶层 table 结构未登记**（已补）
- 8/25 上午：WebBridge status tool 报错 → **缺口：可用 tool 清单未登记**（已补）
